---
up:
  - "[[写题]]"
related:
date: 2026-03-08
---
# 定义与物理图景

- **系统描述**：系统不仅可以与热库交换能量，还可以通过某种机制（如多孔隔板或相变）与粒子库交换粒子。
- **约束条件**：体积 $V$、温度 $T$ 和化学势 $\mu$ 是恒定的；而能量 $E$ 和粒子数 $N$ 是波动的。

使用于开放系统、相平衡问题、量子统计


# 巨配分函数

定义
$$\Xi(V,T,\mu) = \sum_{N=0}^{\infty} e^{ \beta N\mu } Z_{N}(V,T) = \sum_{N,E} e^{ -\beta(E_{r}-\mu N) } $$
它是对所有可能的粒子数 $N$ 以及每个 $N$ 下所有可能的能级 $r$ 的加权求和。

有时专门把新增部分拿出来，称为“逸度”
$$z = e^{ \beta \mu } $$

# 与宏观热力学量的联系

巨势 $\Phi$ 
$$\Phi = \ln\left[ \sum_{r,s}\exp(-\alpha N_{r} - \beta E_{s}) \right] $$

可以得到这些基本的量
$$\bar{N} = z \left( \frac{\partial \ln\Xi}{\partial z} \right)_{V,T} $$
$$S = \frac{\partial }{\partial T} ( kT\ln \Xi )_{z,V} $$
$$U = -\left( \frac{\partial \ln\Xi}{\partial \beta} \right)_{z,V} $$

# 密度与粒子数的涨落

- **粒子数偏差**：$\langle N^2 \rangle - \langle N \rangle^2 = kT \left( \frac{\partial \bar{N}}{\partial \mu} \right)_{V, T}$
- **物理结论**：在热力学极限下（$N \to \infty$），相对偏差 $\Delta N / \bar{N}$ 趋于 0。这意味着在宏观尺度下，巨正则系综与正则系综的结果是等价的

# 理想气体的实例处理

通过 $\mathcal{Q} = \sum \frac{(z Z_1)^N}{N!} = \exp(z Z_1)$，可以极其简洁地导出 $PV = NkT$ 


$$\mathcal{Q} \xrightarrow{\ln} PV \xrightarrow{\partial/\partial \mu} \bar{N} \xrightarrow{\text{Eliminate } z} P(\bar{N}/V, T)$$