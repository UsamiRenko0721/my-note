---
up:
related:
date: 2026-04-25
---
# 晶体结构

## 第一章 晶体：从观察到公理

### 1.1 晶体的经验定义与数学化

从日常经验看，晶体是几何外形规整、具有光滑解理面的固体。矿物的规则多面体形态——石英的六方柱、石盐的立方体——暗示其内部原子排列具有长程周期序。1900年前后，劳厄（Max von Laue）发现X射线通过晶体后产生离散衍射斑点，直接证实了晶体内部存在三维周期性结构。这一经验事实催生了晶体学的核心公理：

>[!晶体学公理]
存在一个三维实向量空间 $\mathbb{R}^3$ 中的一个离散子群 $\mathcal{T} \subset \mathbb{R}^3$（平移子群），使得晶体在 $\mathcal{T}$ 的作用下不变；且 $\mathcal{T}$ 同构于 $\mathbb{Z}^3$，即它是一个三维格点。

更精确地说，若将晶体中所有原子的位置集合视为 $\mathbb{R}^3$ 的一个点集 $S$，则存在三个线性无关的向量 $\mathbf{a}_1,\mathbf{a}_2,\mathbf{a}_3 \in \mathbb{R}^3$，使得对任意 $\mathbf{r} \in S$ 和任意整数 $n_1,n_2,n_3 \in \mathbb{Z}$，有 $\mathbf{r} + n_1\mathbf{a}_1 + n_2\mathbf{a}_2 + n_3\mathbf{a}_3 \in S$。而反过来，$S$ 的平移对称群恰好是所有这些平移的集合：$\mathcal{T} = \{ \mathbf{R} = n_1\mathbf{a}_1 + n_2\mathbf{a}_2 + n_3\mathbf{a}_3 \mid n_i \in \mathbb{Z} \}$。

这样的集合 $\mathcal{T}$ 称为一个**三维布拉维格子**（Bravais lattice），它构成晶体周期性的骨架。但晶体并不只是格点——实际晶体中每个格点上可以附着“基元”（basis），即一组原子的内部构型。用数学语言：

>[!定义]
>一个理想晶体由**布拉维格子** $\mathcal{L}$ 和一个**基元**（有限点集） $B \subset \mathbb{R}^3$ 给出，整个原子集合为
>$$ S = \bigcup_{\mathbf{R} \in \mathcal{L}} (\mathbf{R} + B) $$

但上述定义仅描述了晶体的平移对称性。一个晶体的完整对称群还包含旋转、反射、反演等正交变换，它们构成所谓的**点群**。整个对称群——**空间群**——是平移子群和点群以某种方式结合而成的半直积。

这部分内容将在后续章节详细展开。现在我们仅指出：晶体学的数学核心是**离散子群** $\mathcal{T} \cong \mathbb{Z}^3$ 与**有限群**（点群）的组合结构，以及这些群在 $\mathbb{R}^3$ 上的几何实现。

### 1.2 格点与晶胞

给定 $\mathcal{T}$ 的一组基 $\mathbf{a}_1,\mathbf{a}_2,\mathbf{a}_3$，其生成的平行六面体
$$ \Omega = \left\{ x_1\mathbf{a}_1 + x_2\mathbf{a}_2 + x_3\mathbf{a}_3 \mid 0 \le x_i < 1 \right\} $$
称为一个**原胞**（primitive cell）。它在平移下无重叠地铺满全空间。原胞的体积为 $V_0 = |\mathbf{a}_1 \cdot (\mathbf{a}_2 \times \mathbf{a}_3)|$。

但原胞不一定具有晶体的宏观对称性。更方便的是选择一个**晶胞**（conventional cell）——通常是一个更大的平行六面体，其边沿晶体的对称轴方向，能直观反映点群对称性。例如面心立方（FCC）晶体的原胞是菱形六面体，但晶胞是一个立方体，包含4个格点。晶胞中实际包含的格点数记为 $Z$，晶胞体积 $V_c = Z V_0$。

以上是晶体结构最基本的几何语言。接下来，我们将引入群论工具，系统分类所有可能的布拉维格子和点群。

---

## 第二章 平移对称性，格子与倒易空间

### 2.1 平移群与格点的等价类

平移子群 $\mathcal{T} \cong \mathbb{Z}^3$ 是一个阿贝尔群，其不可约表示（特征标）在一维复表示下给出。由于 $\mathcal{T}$ 是离散的，其特征标为 $\chi_{\mathbf{k}}(\mathbf{R}) = e^{i\mathbf{k} \cdot \mathbf{R}}$，其中 $\mathbf{k}$ 是实向量，且两个相差**倒格矢**的 $\mathbf{k}$ 给出相同字符。这就自然地引入倒易空间。

>[!定义-倒格子]
>倒易基矢 $\mathbf{b}_1,\mathbf{b}_2,\mathbf{b}_3$ 满足：
>$$ \mathbf{a}_i \cdot \mathbf{b}_j = 2\pi \delta_{ij} $$
>显式地，
>$$ \mathbf{b}_1 = 2\pi \frac{\mathbf{a}_2 \times \mathbf{a}_3}{\mathbf{a}_1 \cdot (\mathbf{a}_2 \times \mathbf{a}_3)} $$
>并循环置换。由所有整数线性组合 $\mathbf{G} = h\mathbf{b}_1 + k\mathbf{b}_2 + l\mathbf{b}_3$ 构成的集合称为**倒易格子**（reciprocal lattice），记作 $\mathcal{L}^*$。

倒易格子 $\mathcal{L}^*$ 与原格子 $\mathcal{L}$ 互为傅里叶对偶：对任意 $\mathbf{R} \in \mathcal{L}$ 和 $\mathbf{G} \in \mathcal{L}^*$，$e^{i\mathbf{G} \cdot \mathbf{R}} = 1$。这一性质是X射线衍射理论的数学基础：衍射极大值出现在 $\Delta \mathbf{k} = \mathbf{G}$ 处。

>[!定义-布里渊区]
>以原点为中心，作所有倒格矢的垂直平分面，将空间划分为从内到外的多层区域，分别成为第一、第二......布里渊区。
>
>围出的最小区域称为**第一布里渊区**。

第一布里渊区是倒易空间中具有“原胞”地位的凸多面体，其体积极小化了一个周期。布里渊区的形状对称性完全由原空间点群决定，是能带理论和拓扑物理的基本舞台。其它的布里渊区可以通过平移得到第一布里渊区，也就是说任何布里渊区的体积一样。

### 2.2 格子的代数分类：从 $\mathbb{Z}^3$ 到布拉维格子

布拉维格子的分类是线性代数与群论的一个精致应用。两个正格子基 $\{\mathbf{a}_i\}$ 和 $\{\mathbf{a}'_i\}$ 描述同一格子当且仅当存在整数矩阵 $M \in \text{GL}(3,\mathbb{Z})$（即行列式为 $\pm 1$），使得 $\mathbf{a}'_j = \sum_i M_{ij} \mathbf{a}_i$。这里的 $\text{GL}(3,\mathbb{Z})$ 是整数系数可逆矩阵群，其行列式为 $\pm 1$，保证了体积不变。

但同一格子可以有不同的基选择。更本质的分类依据是格子的**点群对称性**，即保持格子不变的所有正交变换组成的群。设 $\mathcal{L}$ 为一个布拉维格子，其点群 $\mathcal{P}_{\mathcal{L}}$ 定义为：
$$ \mathcal{P}_{\mathcal{L}} = \{ R \in O(3) \mid R\mathcal{L} = \mathcal{L} \} $$
这里 $O(3)$ 是三维正交群（旋转和反射）。注意 $\mathcal{L}$ 本身是离散集，$R\mathcal{L} = \mathcal{L}$ 意味着对每个基向量，$R$ 将它映到某个整数组合。

两个格子若其点群在 $O(3)$ 中共轭，则认为它们属于同一个**晶系**（crystal system）。若再考虑晶胞中的额外对称性（如体心、面心），则得到布拉维类型（Bravais type）。三维中共有14种布拉维格子，分属**7个晶系**。

| 晶系  |   特征对称性    |            布拉维格子             |
| :-: | :--------: | :--------------------------: |
| 三斜  |     无      |             简单三斜             |
| 单斜  |   一个二次轴    |          简单单斜, 底心单斜          |
| 正交  | 三个相互垂直的二次轴 |    简单正交, 体心正交, 面心正交, 底心正交    |
| 四方  |   一个四次轴    |          简单四方, 体心四方          |
| 立方  |   四个三次轴    | 简单立方, 体心立方 (BCC), 面心立方 (FCC) |
| 三方  |   一个三次轴    |           三方 (菱面体)           |
| 六方  |   一个六次轴    |              六方              |

### 2.3 一种纤维丛的类比

在Nakahara的视野中，我们可以将 $\mathbb{R}^3$ 看作基底流形，而晶体结构是其上的一个**周期性的纤维化**：每个原胞就像一个纤维，内部是基元的原子排布。平移对称性意味着整个晶体是原胞空间 $T^3 = \mathbb{R}^3 / \mathcal{L}$（三维环面）上的一个“层”。这种观点在高维、准晶和拓扑物态中很有用，但此处仅作提示。

---

## 第三章 点群

### 3.1 晶体学限制定理

点群是 $O(3)$ 的有限子群，代表保持空间某一点（通常取格点）不变的对称操作。但对一个布拉维格子 $\mathcal{L}$ 来说，点群元素必须将格点映射到格点，这施加了极强的限制以原点为中心，**任何旋转的阶只能是 $1,2,3,4,6$**。这一著名结论称为**晶体学限制定理**。

>[!证明]
>设 $R$ 是绕某轴旋转 $\theta = 2\pi/n$ 的转动。选取垂直于转轴方向的一个最短格矢 $\mathbf{t}$。考虑 $\mathbf{t}$ 的像 $R\mathbf{t}$，以及其与 $\mathbf{t}$ 的差：
>$$ R\mathbf{t} - \mathbf{t} $$
>它也一定是格矢（因为 $R\mathbf{t} \in \mathcal{L}$，$\mathbf{t} \in \mathcal{L}$）。其长度为 $2|\mathbf{t}|\sin(\pi/n)$。若 $\mathbf{t}$ 是最短非零格矢，则必须有 $2\sin(\pi/n) \ge 1$（否则比 $\mathbf{t}$ 还短）或 $2\sin(\pi/n) = 0$（平凡）。由此得到 $\sin(\pi/n) \ge 1/2$，于是 $n=1,2,3,4,6$。$n=5$ 和 $n>6$ 被排除。

### 3.2 点群的有限子群分类

三维 $O(3)$ 的有限子群分为三类：循环群 $C_n$、二面体群 $D_n$（$n=1,2,3,4,6$），以及五个多面体群：四面体群 $T$（阶12）、八面体群 $O$（阶24）、二十面体群 $I$（阶60），以及它们的全旋转版本（包含反演）$T_h, O_h, I_h$ 等。但是晶体点群只允许 $n=1,2,3,4,6$ 的 $C_n$ 和 $D_n$。多面体群中，$T$ 和 $O$ 以 $n=3,4$ 的形式出现（四面体和八面体对称），而二十面体群 $I$ 包含五重对称轴，因此不出现在晶体中（准晶体例外，但那是后话）。

由此，**三维晶体点群共有32种**。列表如下：

- **三斜晶系**：$C_1$，$C_i$（$i$ 表示反演）
- **单斜晶系**：$C_2$，$C_s$（镜面），$C_{2h}$
- **正交晶系**：$D_2$，$C_{2v}$，$D_{2h}$
- **四方晶系**：$C_4$，$S_4$，$C_{4h}$，$D_4$，$C_{4v}$，$D_{2d}$，$D_{4h}$
- **三方晶系**：$C_3$，$C_{3i}$（$S_6$），$D_3$，$C_{3v}$，$D_{3d}$
- **六方晶系**：$C_6$，$C_{3h}$，$C_{6h}$，$D_6$，$C_{6v}$，$D_{3h}$，$D_{6h}$
- **立方晶系**：$T$，$T_h$，$O$，$T_d$，$O_h$

这32种点群是晶体宏观对称性的完整描述。例如，一个晶体的外形可以具有 $O_h$ 对称（立方体），或者 $D_{6h}$（六方柱）。

### 3.3 点群的群论结构

每个点群可以分解为真旋转子群（行列式 $+1$ 的部分）和可能的反演/镜面组合。纯旋转晶体点群有11种：$C_1,C_2,C_3,C_4,C_6,D_2,D_3,D_4,D_6,T,O$。其余21个点群包含非真旋转（反射或反演），通常表示为 $G = G_{\text{rot}} \times \{E,i\}$ 或 $G = G_{\text{rot}} \oplus i G_{\text{rot}}$ 的某种子群。

点群的作用在格点集合上自然给出表示。对于配位数（最近邻数）的确定、振动模的分类等非常重要。例如，$\Gamma$ 点光学声子的对称性由点群的不可约表示标记。

---

## 第四章 空间群

### 4.1 从点群到空间群：半直积

一个晶体的完全对称操作包括平移和可能带有平移部分的旋转（即螺旋轴）或反射加平移（滑移面）。数学上，空间群 $G$ 是 $\mathbb{R}^3$ 的等距群（欧几里得群 $E(3)$）的一个离散子群，且包含一个三维平移格子 $\mathcal{T}$ 作为正规子群。商群 $G/\mathcal{T}$ 同构于点群 $P$——一个有限的 $O(3)$ 子群。整体结构是半直积：
$$ G = \mathcal{T} \rtimes P $$
半直积的含义：点群操作作用于平移群上，对于 $R \in P$ 和 $\mathbf{t} \in \mathcal{T}$，组合规则为 $(\mathbf{t}_1, R_1)(\mathbf{t}_2, R_2) = (\mathbf{t}_1 + R_1\mathbf{t}_2, R_1 R_2)$，且 $R_1\mathbf{t}_2 \in \mathcal{T}$ 自动成立是因为 $P$ 保持格子不变。

但是，上述 $G = \mathcal{T} \rtimes P$ 仅描述了辛格罗尼空间群（symmorphic space groups），即所有点群操作都可以选择某个原点使得它们不附带平移分量。在更一般的情形中，点群元素可能伴有非格子的分数平移，称为非辛格罗尼空间群。此时空间群不完全是一个半直积，而是 $\mathcal{T}$ 的一个扩张：
$$ 1 \longrightarrow \mathcal{T} \longrightarrow G \longrightarrow P \longrightarrow 1 $$
短正合序列。这种扩张由群上同调 $H^2(P, \mathbb{R}^3/\mathcal{T})$ 的元素分类。物理上，分数平移对应于螺旋轴（旋转$2\pi/n$后沿轴平移 $m/n$ 个周期）和滑移面（反射后平移半个周期）。

### 4.2 螺旋轴与滑移面

**螺旋轴**：对 $n$ 重旋转轴，若旋转 $2\pi/n$ 后接着沿轴平移 $p/n$ 个周期（$p=1,\ldots,n-1$），则对称操作记为 $n_p$。常见的有：$2_1$（二重螺旋轴），$3_1$，$3_2$，$4_1$，$4_2$，$4_3$，$6_1$，$6_2$，$6_3$，$6_4$，$6_5$。例如，蛋白质 α 螺旋结构具有 $5_1$ 对称，但晶体学只允许上述 $n=2,3,4,6$。

**滑移面**：反映操作（镜面）后，沿镜面内某个方向平移半个格矢。符号标记：$a$ 滑移（沿 $\mathbf{a}$ 方向平移 $a/2$），$b$ 滑移，$c$ 滑移，$n$ 滑移（沿面对角线方向平移 $(a+b)/2$），$d$ 滑移（菱形滑移，平移 $(a\pm b)/4$等）。滑移面对晶体衍射系统消光有独特指纹。

### 4.3 230个空间群的分类

将**14个布拉维格子**与**32个点群**在扩张意义上组合，并考虑螺旋轴和滑移面后，得到**230个不同的空间群**。这是晶体学史上的一项伟大成就，由费奥多罗夫（Fedorov）（1890）、舍恩弗利斯（Schoenflies）（1891）和巴洛（Barlow）（1894）独立完成。每个空间群用国际符号（如 $P4_2/mnm$）或舍恩弗利斯符号（如 $D_{4h}^{14}$）标记。

国际符号解读示例：
- $P$：初基点阵（简单格子）
- $4_2$：沿 $c$ 轴有四重螺旋轴（转180°后平移一半）
- $m$：垂直于 $a$ 方向的镜面（第一个m）和垂直于 $a+b$ 方向的镜面（第二个m）
- $n$：滑移面类型

细分：
- 73个辛格罗尼空间群（无分数平移，形式为 $\mathcal{T} \rtimes P$）
- 157个非辛格罗尼空间群

每一个空间群以其独特的操作集区分。这些操作对物理性质（如压电、旋光、非线性光学）施加了严格的约束。例如，无中心对称的空间群才可能具有压电效应。

---

## 第五章 群表示论的应用

### 5.1 波函数与不可约表示

在量子力学中，晶体的哈密顿量 $H$ 在空间群 $G$ 的所有操作下不变。这意味着 $H$ 与 $G$ 的酉表示 $U(g)$ 对易：$[U(g), H] = 0$，$\forall g \in G$。因此，$H$ 的本征态可按 $G$ 的不可约表示分类。

对平移子群 $\mathcal{T}$，布洛赫波函数 $\psi_{\mathbf{k}}(\mathbf{r}) = e^{i\mathbf{k}\cdot\mathbf{r}} u_{\mathbf{k}}(\mathbf{r})$ 属于 $\mathcal{T}$ 的表示 $\chi_{\mathbf{k}}$。当考虑全空间群时，还要处理 $\mathbf{k}$ 在点群操作下的变换。$\mathbf{k}$ 的**星**（star）是 $\{ R\mathbf{k} \mid R \in P \}$。若 $R\mathbf{k} = \mathbf{k} + \mathbf{G}$（模倒格矢），则 $R$ 属于 $\mathbf{k}$ 的**小群**（little group）$P_{\mathbf{k}} \subset P$。空间群的不可约表示由 $\mathbf{k}$ 星及其小群 $P_{\mathbf{k}}$ 的射影表示构造（**诱导表示**）。

**相容性关系**：当 $\mathbf{k}$ 沿布里渊区的高对称线变化时，表示的类型可能分裂或合并。这支配了能带交叉的必然性。例如，在石墨烯的 $K$ 点，小群导致狄拉克锥；在某些拓扑绝缘体中，能带反转由特殊点表示的能级顺序决定。

### 5.2 对称性对物理张量的约束

任何物理性质张量（介电常数、电导率、弹性常数等）必须在晶体点群操作下不变。设晶体点群为 $P$，一个 $n$ 阶张量 $T_{i_1 \cdots i_n}$ 在 $R \in P$ 下变换为：
$$ T'_{i_1 \cdots i_n} = R_{i_1 j_1} \cdots R_{i_n j_n} T_{j_1 \cdots j_n} $$
由晶体对称性要求 $T' = T$。这给出线性方程，仅允许特定形式的张量非零分量。例如：
- 在立方晶系中，三阶张量（压电系数）为零，因此立方晶体不显示压电性。
- 在六方晶系（如石英），介电张量是 $(\varepsilon_{\perp}, \varepsilon_{\perp}, \varepsilon_{\parallel})$ 的形式，具有一个光轴。
- 在中心对称点群中，所有奇阶张量为零，故中心对称晶体不可能有压电、热电（二阶）和倍频效应（二阶非线性）。

这种**Neumann原理**是晶体物理学的基石：“晶体的任何物理性质必须具有晶体所属点群的所有对称性。”

### 5.3 不可约表示的构造 – 一个简单例子

以二维正方晶格的点群 $C_{4v}$ 为例。群元素：恒等 $E$，绕垂直轴旋转 $90^\circ$（$C_4$），$180^\circ$（$C_4^2$），$270^\circ$（$C_4^3$），以及四个镜面（$\sigma_v$ 和 $\sigma_d$）。其不可约表示有五个：一维 $A_1$（全对称）、$A_2$（反对称于 $C_4$）、$B_1$（在 $C_4^2$ 下为 $+1$，在 $C_4$ 下为 $-1$）、$B_2$，以及二维 $E$。这些表示直接决定了 $s,p,d$ 轨道在晶体场中的分裂。例如，在四面体配位场中，$d$ 轨道分裂为 $e_g$ 和 $t_{2g}$。

对空间群，$\mathbf{k}$ 点的小群表示需要包含**波矢群**的**射影表示**，因为 $R \mathbf{k} = \mathbf{k} + \mathbf{G}$ 时会引入相位因子 $e^{-i\mathbf{G}\cdot\mathbf{r}}$，导致表示投影。这一处理是 $\mathbf{k}\cdot\mathbf{p}$ 理论和拓扑能带分类的基础。

---

## 第六章 衍射与倒易空间几何

### 6.1 劳厄条件与构造性干涉

X射线衍射实验是确定晶体结构的主要手段。设入射波矢为 $\mathbf{k}_i$，出射波矢为 $\mathbf{k}_f$，散射矢量 $\mathbf{q} = \mathbf{k}_f - \mathbf{k}_i$。晶体中电子密度 $n(\mathbf{r})$ 是周期函数，其傅里叶级数为：
$$ n(\mathbf{r}) = \sum_{\mathbf{G}} n_{\mathbf{G}} e^{i\mathbf{G}\cdot\mathbf{r}} $$
其中 $\mathbf{G}$ 遍历倒格矢。衍射振幅正比于 $\int n(\mathbf{r}) e^{-i\mathbf{q}\cdot\mathbf{r}} d^3r = \sum_{\mathbf{G}} n_{\mathbf{G}} \delta(\mathbf{q} - \mathbf{G})$。因此强衍射峰出现在 $\mathbf{q} = \mathbf{G}$ 处，即**劳厄条件**：
$$ \mathbf{k}_f - \mathbf{k}_i = \mathbf{G} $$
弹性散射下 $|\mathbf{k}_f| = |\mathbf{k}_i| = 2\pi/\lambda$，这等价于著名的布拉格定律 $2d \sin\theta = n\lambda$。

### 6.2 几何结构因子与系统消光

每个衍射峰（$\mathbf{G}$）的强度正比于 $|F_{\mathbf{G}}|^2$ 乘以偏振因子等。$F_{\mathbf{G}}$ 是**结构因子**：
$$ F_{\mathbf{G}} = \sum_{j=1}^{N} f_j(\mathbf{G}) e^{i\mathbf{G}\cdot\mathbf{r}_j} $$
这里 $\mathbf{r}_j$ 是原胞内第 $j$ 个原子的位置，$f_j$ 为原子散射因子。对于某些 $\mathbf{G}$，$F_{\mathbf{G}} = 0$，即使满足布拉格条件也不出现衍射，称为**系统消光**。这是鉴别空间群的关键证据。例如：
- 体心立方（BCC）：$F_{\mathbf{G}} \propto 1 + e^{i\pi(h+k+l)}$，故当 $h+k+l$ 为奇数时消光。
- 面心立方（FCC）：$F_{\mathbf{G}} \propto 1 + e^{i\pi(h+k)} + e^{i\pi(k+l)} + e^{i\pi(l+h)}$，仅当 $h,k,l$ 全为奇数或全为偶数时非零。
- 金刚石结构（两个原子基元，如硅）：消光条件更加精细（当 $h+k+l=4n+2$ 时消光），反映了滑移面对称性。

通过搜集所有衍射线的位置（得到晶胞参数和点阵类型）和消光规律（得到空间群的螺旋轴/滑移面信息），可以唯一确定空间群。

---

## 第七章 晶体结构实例

### 7.1 氯化钠（NaCl）结构

NaCl 晶体：FCC 布拉维格子，基元为 Cl⁻ 位于 (0,0,0) 和 Na⁺ 位于 (1/2,0,0)（以及FCC 平移生成的全部）。空间群为 $Fm\overline{3}m$（$O_h^5$）。点群为 $O_h$，具有中心对称。衍射图样：由于 Na⁺ 和 Cl⁻ 散射因子不同，$hkl$ 全奇或全偶时均出现，但强度有调制。NaCl 结构是许多离子晶体的原型。

### 7.2 钙钛矿（$ABX_3$）结构

以 SrTiO₃ 为例：简单立方格子（但可以有畸变），A 离子在角顶，B 离子在体心（Ti），X 离子在面心（O）。理想立方相空间群 $Pm\overline{3}m$。低温下常发生八面体倾转，对称性降低为正交或四方晶系，形成复杂的畴结构。钙钛矿的铁电性（如 BaTiO₃）来源于 Ti 离子偏离中心的位移，对称性破缺点群从 $O_h$ 降到 $C_{4v}$ 等。

### 7.3 石墨与拓扑相关的结构

石墨的层状结构：六方晶系，空间群 $P6_3/mmc$。单层石墨烯是六方布拉维格子，基元包含两个碳原子（A、B子格），空间群为 $P6m2$（二维）。石墨烯的能带在 $K$ 点具有狄拉克锥，其拓扑性质（贝里相位为 $\pi$）可直接追溯到其晶格对称性及子格对称性（手征对称性）。

### 7.4 准周期晶体——超越经典的数学

1984年，Shechtman 等人发现 Al-Mn 合金的电子衍射图具有五重对称性，这是传统晶体学所禁止的。随之引入了**准晶体**概念：其傅里叶谱由有限个基频的整数组合构成，但平移对称群不再是 $\mathbb{Z}^3$，而是一个更高维 $\mathbb{Z}^n$（$n>3$）在无理投影下的像。著名的彭罗斯拼贴（Penrose tiling）是二维准晶的例子，具有五次对称性。准晶的分类涉及更高维空间群，数学上属于**非周期序**和**投影方法**。尽管超过了经典晶体学范围，但为了完整性必须提及，因为它极大地拓展了“晶体”的数学定义（国际晶体学联合会1992年重新定义晶体：任何具有清晰衍射图的固体，不要求周期性）。

---

## 第八章 对称性分类的现代视角
### 8.1 晶体的对称性与拓扑绝缘体

最近二十年的重大发现是：晶体的对称性不仅限制了能带结构，还可以用来定义新的拓扑不变量的。这些不变量（如**镜面陈数**、**滑移面保护的拓扑态**）超越了传统的 TKNN 陈数。例如，在镜面对称性下，布里渊区中镜面不变的平面（$k_z=0$ 和 $k_z=\pi$）可以分别定义二维陈数 $C_{\pm}$，它们的组合给出镜面陈数 $\eta$。

**拓扑晶体绝缘体**（TCI）如 SnTe，具有镜面陈数 $\nu_{mirror}=1$，导致表面态出现在镜面投影区域。这种分类依赖于空间群/点群的详细结构，是“对称性指标”理论的核心内容。该理论用群上同调和莫尔（Moore）谱序列等方法，对230个空间群分别计算了所有可能的拓扑相。

### 8.2 非辛格罗尼空间群中的拓扑相

螺旋轴与滑移面可以产生独特的拓扑相。例如，$C_2$ 螺旋轴可以保护“高阶拓扑绝缘体”，其铰链处存在一维手性态。已发现一些材料（如 ZrTe₅）在特定空间群 $P4/nmm$ 下具有此类性质。这体现了经典晶体学对称性知识在现代凝聚态物理前沿的复兴。

### 8.3 总结：数学的统一性

从1850年的晶体学点群，到1890年空间群完整列表，再到2020年对称性指标与拓扑材料的完整枚举——晶体结构的数学原理始终是群论在自然科学中最辉煌的应用之一。它既古老，又年轻；既具体，又抽象。正如 Nakahara 所说，“物理理论的优雅往往与对称性深度成正比。”晶体的对称性正是这种优雅的一个高台。

---

## 致谢与进一步阅读

本文写作参照了以下经典与现代文献：
1.  C. Kittel, *Introduction to Solid State Physics* (8th ed.) – 第1-2章提供物理背景。
2.  N. W. Ashcroft & N. D. Mermin, *Solid State Physics* – 第4-7章详尽讨论晶体结构与对称性。
3.  T. Hahn (Ed.), *International Tables for Crystallography, Vol. A: Space-Group Symmetry* – 230个空间群的权威参考。
4.  M. S. Dresselhaus, G. Dresselhaus, & A. Jorio, *Group Theory: Application to the Physics of Condensed Matter* – 群论应用的完整阐述。
5.  B. A. Bernevig & T. L. Hughes, *Topological Insulators and Topological Superconductors* – 第8-10章关于晶体对称性与拓扑相。

读者若希望进一步深入数学背景，可参阅 M. Nakahara *Geometry, Topology, and Physics* 的第1-5章（关于群论基础）以及第13章（关于 Chern 类与凝聚态物理）。

---
# 原子动力学：晶格振动与声子理论

> **本文的核心数学对象**：一个定义在布里渊区环面 $T^3$ 上的 Hermitian 矩阵族
> $$\mathbf{D}: T^3 \to \text{Herm}^+(3r), \quad \mathbf{k} \mapsto \mathbf{D}(\mathbf{k})$$
> 称为**动力学矩阵**。其谱 $\{\omega_s^2(\mathbf{k})\}_{s=1}^{3r}$ 给出声子色散关系；其本征矢 $\{\mathbf{e}_s(\mathbf{k})\}$ 给出简正模的极化方向。整个 Part II 围绕 $\mathbf{D}(\mathbf{k})$ 的构造、对角化、对称性约化、谱几何与统计力学展开。

---

## 前置：从 Part I 继承的结构

在 Part I 中，我们建立了晶体结构的群论语言：
- 布拉维格子 $\mathcal{L} \cong \mathbb{Z}^3$，原胞基 $\{\mathbf{a}_i\}$
- 倒格子 $\mathcal{L}^*$，布里渊区 $T^3 = \mathbb{R}^3 / \mathcal{L}^*$
- 空间群 $G = \mathcal{T} \rtimes P$（或非辛格罗尼扩张）
- $P_{\mathbf{k}} \subset P$：波矢 $\mathbf{k}$ 的小群（保持 $\mathbf{k}$ 模 $\mathcal{L}^*$ 不变的子群）

Part II 的核心任务是：**将原子位移视为 $\mathcal{L}$ 上的向量场，通过 Bloch-Floquet 变换对角化平移对称性，得到 $T^3$ 上的 Hermitian 本征问题**。

---

## 第一章 绝热近似与简谐展开

### 1.1 Born-Oppenheimer 绝热近似

固体是原子核（质量 $M_I \sim 10^3$–$10^5 \, m_e$）与电子（质量 $m_e$）的耦合量子系统。总哈密顿量：

$$
H_{\text{tot}} = T_e + T_N + V_{ee} + V_{eN} + V_{NN}
$$

定义电子哈密顿量（固定核构型 $\{\mathbf{R}_I\}$）：

$$
H_{\text{el}}(\{\mathbf{R}_I\}) = T_e + V_{ee} + V_{eN}(\{\mathbf{R}_I\})
$$

其基态能量 $E_0(\{\mathbf{R}_I\})$ 作为核构型的函数，加上核间库仑排斥，构成**势能面**（potential energy surface）：

$$
U(\{\mathbf{R}_I\}) = E_0(\{\mathbf{R}_I\}) + \frac{1}{2} \sum_{I \neq J} \frac{Z_I Z_J e^2}{|\mathbf{R}_I - \mathbf{R}_J|}
$$

核在此势能面上运动，运动方程由 $M_I \ddot{\mathbf{R}}_I = -\nabla_I U$ 给出。Born-Oppenheimer 近似的数学本质是：**核与电子的时间尺度分离**（$\omega_{\text{el}} / \omega_{\text{phonon}} \sim \sqrt{M_I/m_e} \gg 1$），因此电子绝热地追随核的慢运动。

### 1.2 力常数：Hessian 作为对称双线性型

记第 $I$ 个原胞中第 $\kappa$ 个原子（质量 $M_\kappa$）的平衡位置为 $\mathbf{R}_{I\kappa}^0 = \mathbf{R}_I + \mathbf{d}_\kappa$，瞬时位移为 $\mathbf{u}_{I\kappa}$。在平衡位形附近对势能做 Taylor 展开：

$$
U = U_0 + \sum_{I\kappa i} \underbrace{\frac{\partial U}{\partial u_{I\kappa i}}\Big|_{\text{eq}}}_{=0} u_{I\kappa i} + \frac{1}{2} \sum_{I\kappa i,\, J\lambda j} \Phi_{I\kappa i,\, J\lambda j} \, u_{I\kappa i} \, u_{J\lambda j} + \mathcal{O}(u^3)
$$

其中一阶项为零（平衡条件：合力为零）。二阶系数

$$
\boxed{\Phi_{I\kappa i,\, J\lambda j} = \frac{\partial^2 U}{\partial u_{I\kappa i} \, \partial u_{J\lambda j}}\Bigg|_{\text{eq}}}
$$

称为**力常数矩阵**（force constant matrix）。它是一个 $3N_{\text{at}} \times 3N_{\text{at}}$ 的实对称矩阵，并且在平衡位形下是**正半定**的（势能极小值条件）。从数学视角看，$\Phi$ 是定义在离散空间 $\mathcal{L} \times \{\text{基元原子}\}$ 上的**对称双线性型**。

**简谐近似**：舍去 $\mathcal{O}(u^3)$ 及以上高阶项。在此近似下，运动是 $3rN$ 个耦合谐振子的线性动力学。

### 1.3 对称性对力常数的约束

力常数矩阵并非任意——它必须继承晶体的全部空间群对称性。具体地：

> **平移不变性**：$\Phi_{I\kappa i,\, J\lambda j} = \Phi_{\kappa i,\, \lambda j}(\mathbf{R}_I - \mathbf{R}_J)$ 仅依赖于原胞差值。等价地说，$\Phi$ 是格点上的**卷积核**。

> **刚性平移不变性**：整体平移 $\mathbf{u}_{I\kappa} = \mathbf{u}$（零波矢声学模）不产生恢复力，故
> $$\sum_{J\lambda} \Phi_{\kappa i,\, \lambda j}(\mathbf{R}) = 0 \quad \forall \kappa, i, j$$
> 这一条件保证了 $\omega_s(\mathbf{k}) \to 0$ 当 $\mathbf{k} \to 0$（Goldstone 模式）。

> **空间群等变性**：对任意空间群操作 $g = \{R|\mathbf{t}\} \in G$，
> $$\Phi_{\kappa i,\, \lambda j}(\mathbf{R}) = \sum_{i'j'} R_{ii'} R_{jj'} \, \Phi_{\kappa' i',\, \lambda' j'}(R^{-1}\mathbf{R})$$
> 其中 $(\kappa', \lambda')$ 是 $(\kappa, \lambda)$ 在点群操作 $R$ 下的像（若 $R$ 将 $\kappa$ 原子映射到 $\kappa'$ Wyckoff 位置）。这一条件将在第五章中被系统地利用，通过群表示论约化动力学矩阵。

以上三条约束大幅减少了独立力常数的个数。例如，立方晶系中仅需 2–3 个独立力常数即可参数化最近邻简谐模型。

---

## 第二章 一维原子链：教学案例

> **本章目的**：在一维这一最简单设定中完整展示 Bloch 分解、色散关系、声学/光学支的起源。后续三维理论的所有概念都在这里有了原型。

### 2.1 一维单原子链：$\mathbb{Z}$ 上的离散波动方程

考虑无穷长一维原子链，原子质量均为 $m$，平衡间距 $a$，仅最近邻简谐相互作用（力常数 $K$）。位移记为 $u_n$。**平移群 $\mathbb{Z}$ 作用于指标 $n$**，且运动方程具有平移不变性。

运动方程：

$$
m\ddot{u}_n = K(u_{n+1} - 2u_n + u_{n-1})
$$

右侧可视为 $u_n$ 上的离散拉普拉斯算符 $-\Delta_{\text{disc}}$ 的作用。由于 $\mathbb{Z}$ 是阿贝尔群，其不可约表示为 $e^{ikna}$（$k \in [-\pi/a, \pi/a]$）。Bloch 试探解 $u_n = A e^{i(kna - \omega t)}$ 代入得：

$$
-m\omega^2 = K(e^{ika} + e^{-ika} - 2) = -2K(1 - \cos ka)
$$

色散关系：

$$
\boxed{\omega(k) = \sqrt{\frac{2K}{m}(1 - \cos ka)} = 2\sqrt{\frac{K}{m}}\left|\sin\frac{ka}{2}\right|}
$$

此即 $S^1$（圆周 $k \in [-\pi/a, \pi/a]$ 模 $2\pi/a$）上的标量函数。它是周期函数 $\omega(k + 2\pi/a) = \omega(k)$，这就是一维布里渊区的起源。

**分析**：
- $k \to 0$：$\omega(k) \approx a\sqrt{K/m}\,|k|$，声速 $c = a\sqrt{K/m}$。这是长波连续极限，微观离散性消失。
- $k = \pm \pi/a$（布里渊区边界）：$\omega_{\max} = 2\sqrt{K/m}$，群速度 $d\omega/dk = 0$（驻波）。
- 仅有一支——**声学支**。$\omega \to 0$ 当 $k \to 0$，对应 Goldstone 模式（平移对称性自发破缺）。

群的视角：在这一维问题中，动力学矩阵退化为 $1 \times 1$「矩阵」$D(k) = \frac{K}{m}(2 - e^{ika} - e^{-ika}) = \frac{2K}{m}(1 - \cos ka)$，$\omega^2(k) = D(k)$。

### 2.2 一维双原子链：两带模型

设每个原胞含两个原子，质量 $m_1$ 和 $m_2$（$m_1 < m_2$），原胞长度 $a$。这是最简单的「多带」系统。

在平移群作用下，原胞内的两个原子自由度的动力学矩阵变为 $2 \times 2$ Hermitian 矩阵。设位移 $u_n$（轻原子）和 $v_n$（重原子），仅最近邻力常数 $K$。经过与单原子链相同的 Bloch 分解步骤，久期方程为：

$$
\begin{vmatrix}
\frac{2K}{m_1} - \omega^2 & -\frac{K}{m_1}(1 + e^{-ika}) \\
-\frac{K}{m_2}(1 + e^{ika}) & \frac{2K}{m_2} - \omega^2
\end{vmatrix} = 0
$$

令约化质量 $\mu = m_1 m_2 / (m_1 + m_2)$，解得两支：

$$
\boxed{\omega_{\pm}^2(k) = K\left(\frac{1}{m_1} + \frac{1}{m_2}\right) \pm K\sqrt{\left(\frac{1}{m_1} + \frac{1}{m_2}\right)^2 - \frac{4}{m_1 m_2}\sin^2\frac{ka}{2}}}
$$

**物理图像**：
- **声学支** ($\omega_-$)：$k \to 0$ 时 $\omega_- \approx \sqrt{\frac{K}{2(m_1+m_2)}}\, a|k|$。原胞内两原子同相运动（$A/B \to 1$）。Goldstone 模式。
- **光学支** ($\omega_+$)：$k \to 0$ 时 $\omega_+(0) = \sqrt{2K(1/m_1 + 1/m_2)}$。原胞内两原子反相运动（$A/B = -m_2/m_1$），质心静止。若两原子带相反电荷，此模式产生振荡电偶极矩，可与红外光耦合——「光学」之名的由来。
- 在 $k = \pi/a$（区边界），$\omega_-^2 = 2K/m_2$，$\omega_+^2 = 2K/m_1$，两支分离度最大。
- 频率带隙：$[\omega_-(\pi/a), \omega_+(0)]$ 内不存在传播模式。

**群论解读**：在 $k = 0$（$\Gamma$ 点），波矢群是整个一维点群 $C_s$（仅含镜面反射关于垂直于链的平面）。声学支按恒等表示 $A'$ 变换（同相），光学支按 $A''$ 变换（反相）。这一简单的不可约表示分解，就是三维 $\Gamma$ 点声子分类的原型。

---

## 第三章 动力学矩阵与 Bloch-Floquet 分解

### 3.1 运动方程：力常数作为卷积核

推广到三维。系统有 $N$ 个原胞，每个原胞 $r$ 个原子，总自由度 $3rN$。位移分量 $u_{I\kappa i}$（$i = x,y,z$）的运动方程：

$$
M_\kappa \ddot{u}_{I\kappa i} = -\sum_{J\lambda j} \Phi_{\kappa i,\, \lambda j}(\mathbf{R}_I - \mathbf{R}_J) \, u_{J\lambda j} \tag{3.1}
$$

其中我们已利用了力常数的平移不变性：$\Phi$ 仅依赖于**原胞差** $\mathbf{R} = \mathbf{R}_I - \mathbf{R}_J$。方程 (3.1) 是 $\ell^2(\mathcal{L}) \otimes \mathbb{C}^{3r}$ 上的离散卷积方程。平移不变性允许我们通过 Fourier 变换将其对角化——这正是 Bloch-Floquet 分解的核心。

### 3.2 Bloch-Floquet 变换

引入质量加权位移 $\tilde{u}_{I\kappa i} = \sqrt{M_\kappa} \, u_{I\kappa i}$，方程化为：

$$
\ddot{\tilde{u}}_{I\kappa i} = -\sum_{J\lambda j} \underbrace{\frac{\Phi_{\kappa i,\, \lambda j}(\mathbf{R}_I - \mathbf{R}_J)}{\sqrt{M_\kappa M_\lambda}}}_{=: \tilde{\Phi}_{\kappa i,\, \lambda j}(\mathbf{R}_I - \mathbf{R}_J)} \, \tilde{u}_{J\lambda j} \tag{3.2}
$$

由于平移不变性，$\tilde{\Phi}$ 是格点上的卷积核。Fourier 变换（离散 → 连续）将其对角化。定义：

$$
\tilde{u}_{I\kappa i} = \frac{1}{\sqrt{N}} \sum_{\mathbf{k} \in \text{BZ}} e^{i\mathbf{k}\cdot\mathbf{R}_I} \, \tilde{u}_{\kappa i}(\mathbf{k}) \tag{3.3}
$$

其中 $\mathbf{k}$ 遍历第一布里渊区（受周期性边界条件离散化为 $N$ 个点）。这是标准的 **Bloch-Floquet 分解**：平移群 $\mathcal{T} \cong \mathbb{Z}^3$ 作用在指标 $I$ 上，其不可约表示 $e^{i\mathbf{k}\cdot\mathbf{R}_I}$ 对角化卷积。

代入 (3.2)，得到 $\mathbf{k}$ 空间的运动方程：

$$
\ddot{\tilde{u}}_{\kappa i}(\mathbf{k}) = -\sum_{\lambda j} D_{\kappa i,\, \lambda j}(\mathbf{k}) \, \tilde{u}_{\lambda j}(\mathbf{k}) \tag{3.4}
$$

### 3.3 动力学矩阵的定义

> [!abstract] 定义：动力学矩阵
> 对每个 $\mathbf{k} \in T^3$（布里渊区），**动力学矩阵** $\mathbf{D}(\mathbf{k})$ 是一个 $3r \times 3r$ 的 Hermitian 正半定矩阵：
>
> $$
> \boxed{D_{\kappa i,\, \lambda j}(\mathbf{k}) = \frac{1}{\sqrt{M_\kappa M_\lambda}} \sum_{\mathbf{R} \in \mathcal{L}} \Phi_{\kappa i,\, \lambda j}(\mathbf{R}) \, e^{-i\mathbf{k}\cdot\mathbf{R}}}
> $$
>
> 它是力常数 $\Phi$ 在格点 $\mathcal{L}$ 上的离散 Fourier 变换（除以质量因子）。

设行波解 $\tilde{u}_{\kappa i}(\mathbf{k}, t) = e_{\kappa i}(\mathbf{k}) e^{-i\omega t}$，代入 (3.4) 得本征方程：

$$
\boxed{\omega^2(\mathbf{k}) \, \mathbf{e}(\mathbf{k}) = \mathbf{D}(\mathbf{k}) \, \mathbf{e}(\mathbf{k})} \tag{3.5}
$$

对每个 $\mathbf{k}$，对角化 $\mathbf{D}(\mathbf{k})$ 给出 $3r$ 个本征值 $\omega_s^2(\mathbf{k})$（$s = 1, \dots, 3r$）和相应的正交归一化本征矢 $\mathbf{e}_s(\mathbf{k})$（极化矢量）。

### 3.4 $\mathbf{D}(\mathbf{k})$ 的微分结构

$\mathbf{D}(\mathbf{k})$ 作为 $T^3 \to \text{Herm}^+(3r)$ 的映射具有以下关键性质：

1. **光滑性**：由于力常数 $\Phi(\mathbf{R})$ 随 $|\mathbf{R}|$ 指数衰减（绝缘体）或幂律衰减（金属），Fourier 级数 $\sum_{\mathbf{R}} \Phi(\mathbf{R}) e^{-i\mathbf{k}\cdot\mathbf{R}}$ 在 $T^3$ 上 $C^\infty$（绝缘体）或至少 $C^k$。

2. **周期性**：$\mathbf{D}(\mathbf{k} + \mathbf{G}) = \mathbf{D}(\mathbf{k})$ 对所有倒格矢 $\mathbf{G} \in \mathcal{L}^*$ 成立。因此 $\mathbf{D}$ 自然定义在紧致流形 $T^3$（三维环面）上。

3. **$\mathbf{k} \to 0$ 的声学支行为**：由于刚性平移不变性（§1.3），$\sum_{\lambda j} D_{\kappa i,\, \lambda j}(\mathbf{0}) = 0$。因此 $\mathbf{D}(\mathbf{0})$ 有三个零本征值，对应三个声学支。在 $|\mathbf{k}| \ll |\mathbf{G}|$ 展开：$D_{\kappa i,\, \lambda j}(\mathbf{k}) = D_{\kappa i,\, \lambda j}^{(2)} k_\alpha k_\beta + \mathcal{O}(k^4)$（$D^{(2)}$ 由弹性常数决定）。

4. **对称性**：对 $R \in P_{\mathbf{k}}$（波矢群），
   $$\mathbf{D}(R\mathbf{k}) = \mathbf{\Gamma}(R) \, \mathbf{D}(\mathbf{k}) \, \mathbf{\Gamma}(R)^\dagger$$
   其中 $\mathbf{\Gamma}(R)$ 是 $R$ 在 $3r$ 维位移空间上的酉表示。这使 $\mathbf{D}(\mathbf{k})$ 在 $\mathbf{k}$ 的高对称点/线上可约化为 $P_{\mathbf{k}}$ 不可约表示的直和——见第五章。

> [!tip] 与 Part III 的类比
> $\mathbf{D}(\mathbf{k})$ 之于声子，正如单电子哈密顿量 $H(\mathbf{k}) = e^{-i\mathbf{k}\cdot\mathbf{r}} H e^{i\mathbf{k}\cdot\mathbf{r}}$ 之于电子。两者都是 $T^3$ 上的 Hermitian 算子族，其谱决定元激发的色散，其本征矢决定「波函数」在内部自由度上的结构。Part III 中的贝里曲率、陈数等拓扑概念对声子同样适用。

---

## 第四章 简正坐标与声子量子化

### 4.1 哈密顿量在简正坐标下的对角化

第三章已将运动方程在 $\mathbf{k}$ 空间中解耦。现在将此对角化提升到哈密顿量层面。

系统哈密顿量在简谐近似下为：

$$
H = \frac{1}{2} \sum_{I\kappa i} M_\kappa \dot{u}_{I\kappa i}^2 + \frac{1}{2} \sum_{I\kappa i,\, J\lambda j} \Phi_{\kappa i,\, \lambda j}(\mathbf{R}_I - \mathbf{R}_J) \, u_{I\kappa i} u_{J\lambda j} \tag{4.1}
$$

引入复简正坐标 $Q_{\mathbf{k}s}(t)$，将位移展开为（使用质量加权 $\tilde{u}$ 更方便）：

$$
u_{I\kappa i}(t) = \frac{1}{\sqrt{N M_\kappa}} \sum_{\mathbf{k}s} e_{\kappa i}^{(s)}(\mathbf{k}) \, Q_{\mathbf{k}s}(t) \, e^{i\mathbf{k}\cdot\mathbf{R}_I} \tag{4.2}
$$

其中 $e_{\kappa i}^{(s)}(\mathbf{k})$ 是 $\mathbf{D}(\mathbf{k})$ 的第 $s$ 个本征矢（极化矢量），满足正交归一性 $\sum_{\kappa i} e_{\kappa i}^{(s)*} e_{\kappa i}^{(s')} = \delta_{ss'}$。利用此正交性以及本征方程 $\mathbf{D}(\mathbf{k}) \mathbf{e}_s(\mathbf{k}) = \omega_s^2(\mathbf{k}) \mathbf{e}_s(\mathbf{k})$，得：

$$
\boxed{H = \frac{1}{2} \sum_{\mathbf{k}s} \left( |\dot{Q}_{\mathbf{k}s}|^2 + \omega_s^2(\mathbf{k}) |Q_{\mathbf{k}s}|^2 \right)} \tag{4.3}
$$

哈密顿量被对角化为 $3rN$ 个独立的简谐振子。运动方程退化为 $\ddot{Q}_{\mathbf{k}s} + \omega_s^2(\mathbf{k}) Q_{\mathbf{k}s} = 0$。

### 4.2 直接积分分解视角

数学上更优雅的表述是：将 $\mathcal{H} = \ell^2(\mathcal{L}) \otimes \mathbb{C}^{3r}$ 视为 Hilbert 空间，利用平移群 $\mathcal{T} \cong \mathbb{Z}^3$ 的阿贝尔性，通过 Bloch-Floquet 变换做**直接积分分解**（direct integral decomposition）：

$$
\mathcal{H} \cong \int_{T^3}^{\oplus} \mathbb{C}^{3r} \, d\mu(\mathbf{k})
$$

在此分解下，哈密顿量成为纤维化的：

$$
H \cong \int_{T^3}^{\oplus} \begin{pmatrix} \mathbf{1} & 0 \\ 0 & \mathbf{D}(\mathbf{k}) \end{pmatrix} d\mu(\mathbf{k})
$$

其中 $\mu$ 是布里渊区上的归一化测度 $d\mu(\mathbf{k}) = V_{\text{BZ}}^{-1} d^3k$。这完全平行于 Part III 中的 Bloch-Floquet 分解 $H_{\text{el}} \cong \int_{T^3}^{\oplus} H(\mathbf{k}) \, d\mathbf{k}$。

### 4.3 量子化与声子 Fock 空间

将 $Q_{\mathbf{k}s}$ 及其共轭动量 $P_{\mathbf{k}s} = \dot{Q}_{\mathbf{k}s}^*$ 提升为算符，施加正则对易关系：

$$
[\hat{Q}_{\mathbf{k}s}, \hat{P}_{\mathbf{k}'s'}^\dagger] = i\hbar \delta_{\mathbf{k}\mathbf{k}'} \delta_{ss'}
$$

定义升降算符（产生/湮灭算符）：

$$
\hat{Q}_{\mathbf{k}s} = \sqrt{\frac{\hbar}{2\omega_s(\mathbf{k})}} \left( \hat{a}_{\mathbf{k}s} + \hat{a}_{-\mathbf{k}s}^\dagger \right), \quad
\hat{P}_{\mathbf{k}s} = -i \sqrt{\frac{\hbar\omega_s(\mathbf{k})}{2}} \left( \hat{a}_{\mathbf{k}s} - \hat{a}_{-\mathbf{k}s}^\dagger \right) \tag{4.4}
$$

满足**玻色对易关系**：

$$
[\hat{a}_{\mathbf{k}s}, \hat{a}_{\mathbf{k}'s'}^\dagger] = \delta_{\mathbf{k}\mathbf{k}'} \delta_{ss'}, \quad [\hat{a}, \hat{a}] = [\hat{a}^\dagger, \hat{a}^\dagger] = 0 \tag{4.5}
$$

量子哈密顿量：

$$
\boxed{\hat{H} = \sum_{\mathbf{k}s} \hbar\omega_s(\mathbf{k}) \left( \hat{a}_{\mathbf{k}s}^\dagger \hat{a}_{\mathbf{k}s} + \frac{1}{2} \right)} \tag{4.6}
$$

**Fock 空间**：真空态 $|0\rangle$ 满足 $\hat{a}_{\mathbf{k}s}|0\rangle = 0$。多声子态：

$$
|\{n_{\mathbf{k}s}\}\rangle = \prod_{\mathbf{k}s} \frac{(\hat{a}_{\mathbf{k}s}^\dagger)^{n_{\mathbf{k}s}}}{\sqrt{n_{\mathbf{k}s}!}} |0\rangle \tag{4.7}
$$

能量本征值为 $E = \sum_{\mathbf{k}s} \hbar\omega_s(\mathbf{k}) (n_{\mathbf{k}s} + \frac{1}{2})$。

### 4.4 位移算符：量子场的模展开

位移算符（海森堡绘景）写为：

$$
\boxed{\hat{u}_{I\kappa i}(t) = \frac{1}{\sqrt{N M_\kappa}} \sum_{\mathbf{k}s} \sqrt{\frac{\hbar}{2\omega_s(\mathbf{k})}} \, e_{\kappa i}^{(s)}(\mathbf{k}) \left( \hat{a}_{\mathbf{k}s} e^{i(\mathbf{k}\cdot\mathbf{R}_I - \omega_s t)} + \hat{a}_{\mathbf{k}s}^\dagger e^{-i(\mathbf{k}\cdot\mathbf{R}_I - \omega_s t)} \right)} \tag{4.8}
$$

这正是**实标量量子场**在紧致空间 $T^3$ 上的模展开。每个 $(\mathbf{k}, s)$ 模式是一个独立的量子谐振子；声子是这些谐振子的元激发。

---

## 第五章 声子色散的群论分类

> **本章是 Part II 数学化的核心。**Part I 建立的空间群表示论在此被完整应用于声子问题：对布里渊区每个 $\mathbf{k}$ 点，动力学矩阵 $\mathbf{D}(\mathbf{k})$ 可按波矢群 $P_{\mathbf{k}}$ 的不可约表示块对角化，从而预言简并度、选择定则以及色散关系沿高对称线的连接方式。

### 5.1 波矢群与动力学矩阵的块对角化

对给定的 $\mathbf{k} \in \text{BZ}$，**波矢群**（小群）为：

$$
P_{\mathbf{k}} = \{ R \in P \mid R\mathbf{k} = \mathbf{k} + \mathbf{G}, \; \mathbf{G} \in \mathcal{L}^* \}
$$

即点群 $P$ 中保持 $\mathbf{k}$ 模倒格矢不变的元素。对 $R \in P_{\mathbf{k}}$，对称性要求（参见 §1.3）：

$$
\mathbf{D}(R\mathbf{k}) = \mathbf{D}(\mathbf{k} + \mathbf{G}) = \mathbf{D}(\mathbf{k}) = \mathbf{\Gamma}(R) \, \mathbf{D}(\mathbf{k}) \, \mathbf{\Gamma}(R)^\dagger \tag{5.1}
$$

其中 $\mathbf{\Gamma}(R)$ 是 $R$ 在 $3r$ 维位移空间上的表示（酉表示）。方程 (5.1) 意味着 $[\mathbf{D}(\mathbf{k}), \mathbf{\Gamma}(R)] = 0$ 对所有 $R \in P_{\mathbf{k}}$ 成立。因此 **$\mathbf{D}(\mathbf{k})$ 的本征空间是 $P_{\mathbf{k}}$ 的表示空间**，可按 $P_{\mathbf{k}}$ 的不可约表示分解。

具体地，$3r$ 维位移表示 $\mathbf{\Gamma}$ 分解为 $P_{\mathbf{k}}$ 的不可约表示：

$$
\mathbf{\Gamma} = \bigoplus_{\alpha} m_{\alpha} \Gamma^{(\alpha)}
$$

其中 $\Gamma^{(\alpha)}$ 是 $P_{\mathbf{k}}$ 的不可约表示，$m_{\alpha}$ 是重数。声子模式 $(s)$ 按这些不可约表示分类。**同属于一个多维不可约表示的模式在 $\mathbf{k}$ 点必然简并**——这种「对称性强制的简并」是群论预测声子色散的核心机制。

### 5.2 $\Gamma$ 点声子的点群分解：金刚石结构范例

$\Gamma$ 点（$\mathbf{k} = 0$）是最重要的高对称点，其波矢群是整个点群 $P$。此处我们给出一套完整计算，展示群论如何预言声子对称性。

**晶体结构**：金刚石（空间群 $Fd\bar{3}m$，No. 227）——FCC 布拉维格子，基元含两个碳原子位于 $\tau_1 = (0,0,0)$ 和 $\tau_2 = \frac{a}{4}(1,1,1)$（以 FCC 常规晶胞坐标）。点群为 $O_h$。

**位移表示 $\mathbf{\Gamma}_{\text{mech}}$ 的构造**：

第一步：确定两个 Wyckoff 位置在 $O_h$ 下的置换表示。$\tau_1$ 和 $\tau_2$ 通过体对角线中点 $(1/8, 1/8, 1/8)$ 处的反演相关联。在 $O_h$ 的 48 个操作下，这两个位置或者保持不变（若操作将 $\tau_1 \mapsto \tau_1$，则也自动将 $\tau_2 \mapsto \tau_2$），或者互换。置换表示的特征标：

| $O_h$ 类 | $E$ | $8C_3$ | $6C_2$ | $6C_4$ | $3C_4^2$ | $i$ | $8S_6$ | $6\sigma_d$ | $6S_4$ | $3\sigma_h$ |
|----------|-----|--------|--------|--------|----------|-----|--------|-------------|--------|-------------|
| $\chi_{\text{perm}}$ | 2 | 2 | 0 | 0 | 2 | 0 | 0 | 2 | 0 | 2 |

检查 $O_h$ 的特征标表，此置换表示分解为 $\Gamma_{\text{perm}} = A_{1g} \oplus A_{2u}$。物理意义：$A_{1g}$（全对称）对应两原子同相振动；$A_{2u}$（反演下反号）对应反相振动。

第二步：总位移表示 = 置换表示 ⊗ 向量表示 $T_{1u}$（因为每个原子有 $x,y,z$ 三个位移方向）：

$$
\Gamma_{\text{mech}} = (A_{1g} \oplus A_{2u}) \otimes T_{1u} = \underbrace{T_{1u}}_{\text{声学支}} \oplus \underbrace{T_{2g}}_{\text{光学支}}
$$

其中使用了 $O_h$ 群的直积分解规则 $A_{2u} \otimes T_{1u} = T_{2g}$。

**物理解读**：
- **$T_{1u}$（三重简并）**：三个声学支。在 $\Gamma$ 点 $\omega = 0$（Goldstone 模式）。按向量表示变换 → **红外活性**（可与红外光耦合）。
- **$T_{2g}$（三重简并）**：三个光学支。在 $\Gamma$ 点 $\omega \neq 0$。按秩-2 对称张量的反对称部分变换 → **拉曼活性**（可与可见光发生非弹性散射），但因金刚石有中心反演对称，**红外不活性**（电偶极跃迁禁戒）。

这一分析完全来自群论，无需任何具体力常数值。实验上，金刚石的 $\Gamma$ 点光学声子频率约为 $1332 \, \text{cm}^{-1}$（拉曼谱中著名的金刚石峰），单支简并（$T_{2g}$ 三重），与群论预测一致。

> [!example] 对比：闪锌矿结构（GaAs，$\bar{4}3m$）
> GaAs 的原胞同样含两个原子（Ga 和 As），但两者**不同**——无中心反演。点群为 $T_d$（$O_h$ 的纯旋转子群去掉反演）。(Ga, As) 的置换表示仅为 $A_1$（两位置在 $T_d$ 下不再通过反演关联——实际上在 $T_d$ 下两位置是独立的）。位移表示分解为 $\Gamma_{\text{mech}} = T_2 \oplus T_2$。**声学和光学支均按 $T_2$ 变换**，因此光学支既是红外活性也是拉曼活性。这正是极性半导体（GaAs、InP 等）的典型特征。

### 5.3 对称性低的高对称点

当 $\mathbf{k} \neq 0$ 时，波矢群 $P_{\mathbf{k}} \subset P$ 更小。例如在 FCC 晶格中：
- $X$ 点（$\mathbf{k} = \frac{2\pi}{a}(1,0,0)$）：$P_X \cong D_{4h}$（16 阶）。$T_{1u}$ 表示在此子群下分裂，声学简并度降低。
- $L$ 点（$\mathbf{k} = \frac{2\pi}{a}(\frac{1}{2},\frac{1}{2},\frac{1}{2})$）：$P_L \cong D_{3d}$（12 阶）。

精确的分解需要 $P_{\mathbf{k}}$ 的特征标表和相容性关系，这属于「空间群不可约表示」的标准计算（参考 Dresselhaus 或 Bradley & Cracknell）。

### 5.4 相容性关系与色散连接

沿高对称线 $\mathbf{k}(t)$（例如从 $\Gamma$ 到 $X$），波矢群从 $P_{\Gamma}$ 降为线的波矢群 $P_{\Lambda}$。$\Gamma_{\text{mech}}$ 中每个 $\Gamma$ 点的不可约表示在 $P_{\Lambda}$ 下可能分裂为更低维的表示。**相容性关系**精确定义了这些分裂方式，从而预言了能带沿高对称线的连接方式。

例如，金刚石的 $T_{2g}$ 在沿 $\Gamma \to X$（$\Lambda$ 方向）的方向群 $C_{4v}$ 下分解为 $B_2 \oplus E$（一维 + 二维）。这预言了沿 $\Gamma$–$X$，三重简并光学支分裂为一个单态和一个双重态——与中子散射实验完全一致。

---

## 第六章 声子态密度与 Van Hove 奇点

### 6.1 态密度的积分表示

声子态密度（phonon density of states）$g(\omega)$ 定义为频率在 $[\omega, \omega+d\omega]$ 内的模式数：

$$
g(\omega) = \frac{V}{(2\pi)^3} \sum_{s=1}^{3r} \int_{\text{BZ}} \delta(\omega - \omega_s(\mathbf{k})) \, d^3k \tag{6.1}
$$

在 $T^3$ 的等频面 $\Sigma_\omega = \{\mathbf{k} : \omega_s(\mathbf{k}) = \omega\}$ 上，利用 $\delta$-函数的恒等式：

$$
\boxed{g(\omega) = \frac{V}{(2\pi)^3} \sum_{s=1}^{3r} \oint_{\Sigma_\omega} \frac{dS}{|\nabla_{\mathbf{k}} \omega_s(\mathbf{k})|}} \tag{6.2}
$$

此公式揭示了态密度的核心几何特征：**$g(\omega)$ 由等频面的面积与群速度 $|\nabla_{\mathbf{k}}\omega_s|$ 的反比决定**。当 $|\nabla_{\mathbf{k}}\omega_s| \to 0$ 时，态密度发散——这正是 Van Hove 奇点的起源。

### 6.2 Morse 引理与临界点分类

$\nabla_{\mathbf{k}} \omega_s = 0$ 的点是 $\omega_s$ 在 $T^3$ 上的**临界点**。由 Morse 引理，在非退化临界点附近，$\omega_s$ 可经局部坐标变换化为标准形式：

$$
\omega(\mathbf{q}) = \omega_c + \frac{1}{2} \sum_{\alpha=1}^{3} \lambda_\alpha q_\alpha^2 + \mathcal{O}(q^3)
$$

其中 $\lambda_\alpha$ 是 Hessian 矩阵 $\partial^2\omega_s / \partial k_\alpha \partial k_\beta$ 的本征值。**Morse 指数** $\eta$ 定义为负本征值的个数。

| $\eta$ | 临界点类型 | 三维 $g(\omega)$ 的奇异性 |
|--------|-----------|--------------------------|
| $0$ | 极小值点 | $g(\omega) \propto \sqrt{\omega - \omega_c}$（带边行为，无发散） |
| $1$ | 鞍点（一个下坡方向） | $g(\omega)$ **连续但导数发散**（折点） |
| $2$ | 鞍点（两个下坡方向） | $g(\omega)$ **连续但导数发散**（折点） |
| $3$ | 极大值点 | $g(\omega) \propto \sqrt{\omega_{\max} - \omega}$（带边截断） |

在布里渊区这一紧致流形上，Morse 理论保证临界点必然存在且满足 Morse 不等式。对于典型的声子色散，临界点集中在高对称 $\mathbf{k}$ 点（$\Gamma, X, L, W, K$ 等）。

### 6.3 二维范例：正方晶格的 Van Hove 奇点

为给出显式计算，考虑二维正方晶格（声学支）的简化模型：

$$
\omega^2(\mathbf{k}) = \omega_0^2 \left[ 2 - \cos(k_x a) - \cos(k_y a) \right]
$$

临界点（$\nabla_{\mathbf{k}} \omega = 0$）：
- $\Gamma = (0,0)$：$\omega = 0$，极小值点，$\eta = 0$
- $M = (\pi/a, \pi/a)$：$\omega = 2\omega_0$，极大值点，$\eta = 2$
- $X = (\pi/a, 0)$ 及其对称等价点 $(0, \pi/a)$：$\omega = \sqrt{2}\omega_0$，鞍点，$\eta = 1$

**$X$ 点附近的渐近行为**：在 $X$ 点附近，令 $\mathbf{k} = (\pi/a + q_x, q_y)$，展开至二阶：

$$
\omega^2 \approx 2\omega_0^2 + \omega_0^2 a^2 \frac{q_y^2 - q_x^2}{2}
$$

Hessian 的本征值为 $-\omega_0^2 a^2/2$ 和 $+\omega_0^2 a^2/2$，符号 $(+,-)$。在二维中，Morse 指数为 1 的鞍点给出**对数奇点**：

$$
g(\omega) \sim -\frac{1}{2\pi^2 \omega_0^2 a^2} \ln\left| \omega^2 - 2\omega_0^2 \right| \quad (\omega \to \sqrt{2}\omega_0)
$$

这比三维鞍点的折点奇异更强——是维数降低的直接后果。在二维材料（石墨烯等）的中子散射或电子能谱中，这种对数奇点是范霍夫奇点的典型指纹。

将这一分析推广至三维任意晶格：每一个高对称 $\mathbf{k}$ 点处的 Hessian 决定该临界点对 $g(\omega)$ 的贡献类型。完整的态密度由所有分支在所有临界点处的贡献叠加而成。

---

## 第七章 晶格热容

### 7.1 声子统计与内能

声子是玻色子。在温度 $T$，模式 $(\mathbf{k}s)$ 的平均占据数由 Bose-Einstein 分布给出：

$$
\langle n_{\mathbf{k}s} \rangle = \frac{1}{e^{\beta\hbar\omega_s(\mathbf{k})} - 1}, \quad \beta = \frac{1}{k_B T}
$$

内能（不计零点能）：

$$
U(T) = \sum_{\mathbf{k}s} \frac{\hbar\omega_s(\mathbf{k})}{e^{\beta\hbar\omega_s(\mathbf{k})} - 1} = \int_0^\infty d\omega \, g(\omega) \, \frac{\hbar\omega}{e^{\beta\hbar\omega} - 1}
$$

定容热容：

$$
C_V = \left( \frac{\partial U}{\partial T} \right)_V = k_B \int_0^\infty d\omega \, g(\omega) \left( \frac{\hbar\omega}{k_B T} \right)^2 \frac{e^{\beta\hbar\omega}}{(e^{\beta\hbar\omega} - 1)^2} \tag{7.1}
$$

### 7.2 德拜模型

德拜模型将三支声学模近似为各向同性连续介质中的弹性波：$\omega = c_s k$（$s = L, T_1, T_2$），并在德拜频率 $\omega_D$ 处截断以保持总模式数为 $3N_{\text{at}}$。德拜波矢 $k_D = (6\pi^2 n)^{1/3}$（$n = N_{\text{at}}/V$），平均声速由 $3/c_s^3 = 1/c_L^3 + 2/c_T^3$ 定义。

态密度：

$$
g_D(\omega) = \frac{3V}{2\pi^2 c_s^3} \omega^2, \quad 0 \leq \omega \leq \omega_D
$$

热容（以 $x = \hbar\omega/k_B T$，$\Theta_D = \hbar\omega_D/k_B$）：

$$
C_V = 9N_{\text{at}} k_B \left( \frac{T}{\Theta_D} \right)^3 \int_0^{\Theta_D/T} \frac{x^4 e^x}{(e^x - 1)^2} dx \tag{7.2}
$$

**极限行为**：
- $T \ll \Theta_D$：$C_V \approx \frac{12\pi^4}{5} N_{\text{at}} k_B (T/\Theta_D)^3$——**德拜 $T^3$ 律**
- $T \gg \Theta_D$：$C_V \to 3N_{\text{at}} k_B$——**Dulong-Petit 定律**

### 7.3 一般形式

对于包含光学支的实际晶体，热容可写为德拜贡献（声学支）+ 爱因斯坦贡献（光学支，每支近似为常数频率 $\omega_E$）：

$$
C_V = C_V^{\text{Debye}} + \sum_{\text{光学支}} C_V^{\text{Einstein}}(\omega_E^{(s)})
$$

单个爱因斯坦模的热容：

$$
C_V^{\text{Einstein}}(\omega_E) = N k_B \left( \frac{\hbar\omega_E}{k_B T} \right)^2 \frac{e^{\beta\hbar\omega_E}}{(e^{\beta\hbar\omega_E} - 1)^2}
$$

在高频极限 $k_B T \ll \hbar\omega_E$，$C_V^{\text{Einstein}} \sim N k_B (\hbar\omega_E/k_B T)^2 e^{-\hbar\omega_E/k_B T}$——指数衰减，解释了为何高频光学支对低温比热无贡献。

低温实验中，$C_V = \gamma T + \beta T^3$（金属：$\gamma T$ 为电子贡献，$\beta T^3$ 为声子贡献），精确验证了德拜 $T^3$ 律和独立电子气模型的联合预言。

---

## 第八章 非简谐效应与拓扑声子

### 8.1 非简谐性的起源

简谐近似下，声子不相互作用，因而热导率无限大、无热膨胀。真实晶体中，三阶及更高阶力常数导致：
- **声子-声子散射**：三阶项 $\Phi^{(3)} uuu$ 产生三声子过程（劈裂/融合），通过微扰论给出声子寿命和有限热导率；
- **热膨胀**：格林艾森参数 $\gamma = -\partial\ln\omega / \partial\ln V$ 描述频率随体积（温度）的漂移；
- **频率重整化**：声子自能修正 $\Sigma(\mathbf{k}, \omega)$（在 Matsubara 或 Keldysh 表象中计算）导致温度依赖的峰位移动和展宽。

非简谐理论是严格处理热输运的必由之路（通过声子 Boltzmann 方程或格林函数方法），其数学结构与量子场论中的 Dyson 方程高度平行。

### 8.2 拓扑声子

近十年来，声子的拓扑性质成为前沿。与电子系统平行的概念：

- **声子 Weyl 点**：在两支声子带偶然简并处（$\omega_s(\mathbf{k}) = \omega_{s'}(\mathbf{k})$），色散呈线性，携带量子化的贝里通量（Berry flux）；
- **声子陈数**：在时间反演破缺的磁性绝缘体中，声子可具有非零二阶陈数，导致**声子霍尔效应**（热霍尔效应）；
- **拓扑保护的声子边界态**：在体带隙中出现的无能隙表面/边缘声子模式。

这些现象将 Part I 的空间群对称性与 Part III 的能带拓扑概念融为一体，体现了现代凝聚态物理的统一性。详细内容可参考后续 Part III 第七章后的独立笔记。

---

## 参考文献

1. Ashcroft, N. W., & Mermin, N. D. (1976). *Solid State Physics*. Holt, Rinehart and Winston.（第21–25章）
2. Kittel, C. (2005). *Introduction to Solid State Physics* (8th ed.). Wiley.（第4–5章）
3. Born, M., & Huang, K. (1954). *Dynamical Theory of Crystal Lattices*. Oxford University Press.（经典参考文献，群论分类的起源）
4. Maradudin, A. A., Montroll, E. W., Weiss, G. H., & Ipatova, I. P. (1971). *Theory of Lattice Dynamics in the Harmonic Approximation* (2nd ed.). Academic Press.
5. Dresselhaus, M. S., Dresselhaus, G., & Jorio, A. (2008). *Group Theory: Application to the Physics of Condensed Matter*. Springer.（第6–7章：晶格振动的群论分类，含完整特征标表）
6. Bradley, C. J., & Cracknell, A. P. (1972). *The Mathematical Theory of Symmetry in Solids*. Oxford University Press.（空间群不可约表示的标准参考）
7. Ziman, J. M. (1960). *Electrons and Phonons*. Oxford University Press.（第1章：声子理论基础）
8. Nakahara, M. (2003). *Geometry, Topology, and Physics* (2nd ed.). Taylor & Francis.（第1章：群论基础；第12章：指标定理与拓扑）

# 电子的动力学：能带理论与电子态

## 第一章 引言：周期势场中的电子

在固体物理中，电子系统的核心哈密顿量为：
$$ H = \sum_i \frac{\mathbf{p}_i^2}{2m} + \frac{1}{2}\sum_{i\neq j} \frac{e^2}{|\mathbf{r}_i-\mathbf{r}_j|} + \sum_{i,I} V_{\text{ion}}(\mathbf{r}_i-\mathbf{R}_I) $$
这是一个多体问题。**能带理论**的基本近似是：将电子视为在周期性排列的离子实产生的有效势场中独立运动，电子-电子相互作用被平均场或密度泛函理论吸收进有效势 $V_{\text{eff}}(\mathbf{r})$，满足周期条件 $V_{\text{eff}}(\mathbf{r}+\mathbf{R}) = V_{\text{eff}}(\mathbf{r})$，$\mathbf{R}$ 为布拉维格矢。于是问题化为单电子薛定谔方程：
$$ \left[ -\frac{\hbar^2}{2m}\nabla^2 + V(\mathbf{r}) \right] \psi(\mathbf{r}) = E \psi(\mathbf{r}), \quad V(\mathbf{r}+\mathbf{R})=V(\mathbf{r}). $$

能带理论的目标是求解此方程，找到能量本征值 $E_n(\mathbf{k})$ 作为波矢 $\mathbf{k}$ 的函数（能带结构），并由此计算电子态密度、费米面以及各种输运与光学性质。

---

## 第二章 布洛赫定理与布洛赫函数

### 2.1 平移对称性与布洛赫定理

周期势场 $V(\mathbf{r})$ 具有平移对称性：定义平移算符 $T_{\mathbf{R}} \psi(\mathbf{r}) = \psi(\mathbf{r}+\mathbf{R})$。由于 $[T_{\mathbf{R}}, H] = 0$，我们可以同时对角化 $H$ 和全体 $T_{\mathbf{R}}$。平移子群 $\mathcal{T} = \{\mathbf{R}\}$ 是阿贝尔群，其不可约表示为一维字符 $\chi_{\mathbf{k}}(\mathbf{R}) = e^{i\mathbf{k}\cdot\mathbf{R}}$，其中 $\mathbf{k}$ 是实向量（倒易空间中的波矢）。因此本征函数满足：
$$ T_{\mathbf{R}} \psi(\mathbf{r}) = \psi(\mathbf{r}+\mathbf{R}) = e^{i\mathbf{k}\cdot\mathbf{R}} \psi(\mathbf{r}) $$
这即是**布洛赫定理**。

由此可设解的形式为：
$$ \psi_{\mathbf{k}}(\mathbf{r}) = e^{i\mathbf{k}\cdot\mathbf{r}} u_{\mathbf{k}}(\mathbf{r}) $$
其中 $u_{\mathbf{k}}(\mathbf{r}+\mathbf{R}) = u_{\mathbf{k}}(\mathbf{r})$ 具有晶格周期性。这样的函数称为**布洛赫函数**。$\mathbf{k}$ 称为**晶体动量**，它是好量子数。薛定谔方程化为 $u_{\mathbf{k}}$ 的方程：
$$ \left[ \frac{(-i\hbar\nabla + \hbar\mathbf{k})^2}{2m} + V(\mathbf{r}) \right] u_{\mathbf{k}}(\mathbf{r}) = E(\mathbf{k}) u_{\mathbf{k}}(\mathbf{r}) $$
该方程定义在环面 $\mathbb{R}^3/\mathcal{L}$（原胞）上，具有离散谱，因此对每个 $\mathbf{k}$ 有无穷多本征值 $E_n(\mathbf{k})$，$n$ 为能带指标。

### 2.2 倒易空间与布里渊区

由于 $u_{\mathbf{k}}$ 的周期性，波矢 $\mathbf{k}$ 与 $\mathbf{k}+\mathbf{G}$（$\mathbf{G}$ 为倒格矢）给出相同的物理态，因为 $e^{i(\mathbf{k}+\mathbf{G})\cdot\mathbf{r}} u_{\mathbf{k}+\mathbf{G}}(\mathbf{r}) = e^{i\mathbf{k}\cdot\mathbf{r}} (e^{i\mathbf{G}\cdot\mathbf{r}} u_{\mathbf{k}+\mathbf{G}}(\mathbf{r}))$，括号内仍周期。因此 $\mathbf{k}$ 可限制在第一布里渊区（BZ）内。布里渊区是倒易空间的原胞，其形状由晶格对称性决定。

**群论补充**：波矢 $\mathbf{k}$ 的小群（波矢群）$P_{\mathbf{k}} \subset P$（点群）保持 $\mathbf{k}$ 模倒格矢不变。能带 $E_n(\mathbf{k})$ 在 $\mathbf{k}$ 点处具有 $P_{\mathbf{k}}$ 的对称性，其本征函数按 $P_{\mathbf{k}}$ 的不可约表示变换。

---

## 第三章 近自由电子近似

### 3.1 基本思想与微扰论

对于简单金属（如 Na、Al），价电子感受到的离子势较弱，可视为“近自由”的。将周期势 $V(\mathbf{r})$ 视为对自由电子的微扰。自由电子哈密顿量为 $H_0 = -\hbar^2\nabla^2/(2m)$，本征态为平面波 $|\mathbf{k}\rangle = \frac{1}{\sqrt{V}} e^{i\mathbf{k}\cdot\mathbf{r}}$，能量 $\varepsilon_{\mathbf{k}}^0 = \frac{\hbar^2 k^2}{2m}$。周期势的傅里叶展开：
$$ V(\mathbf{r}) = \sum_{\mathbf{G}} V_{\mathbf{G}} e^{i\mathbf{G}\cdot\mathbf{r}}, \quad V_{\mathbf{G}} = \frac{1}{\Omega} \int_{\text{cell}} V(\mathbf{r}) e^{-i\mathbf{G}\cdot\mathbf{r}} d^3r $$
其中 $V_{\mathbf{G}=0}$ 为常数，可设为零（能量零点）。微扰矩阵元：
$$ \langle \mathbf{k}' | V | \mathbf{k} \rangle = \frac{1}{V} \int e^{-i\mathbf{k}'\cdot\mathbf{r}} V(\mathbf{r}) e^{i\mathbf{k}\cdot\mathbf{r}} d^3r = \sum_{\mathbf{G}} V_{\mathbf{G}} \delta_{\mathbf{k}' - \mathbf{k}, \mathbf{G}} $$
即只有满足 $\mathbf{k}' = \mathbf{k} + \mathbf{G}$ 的平面波之间才有耦合。

### 3.2 非简并微扰与能带结构

当 $\mathbf{k}$ 远离布里渊区边界时，$|\mathbf{k}\rangle$ 与 $|\mathbf{k}+\mathbf{G}\rangle$ 能量差大，可用非简并微扰：
$$ E(\mathbf{k}) \approx \varepsilon_{\mathbf{k}}^0 + V_0 + \sum_{\mathbf{G}\neq 0} \frac{|V_{\mathbf{G}}|^2}{\varepsilon_{\mathbf{k}}^0 - \varepsilon_{\mathbf{k}+\mathbf{G}}^0} $$
修正项分母小则修正大。当 $\mathbf{k}$ 靠近布里渊区边界（即 $\mathbf{k} \approx \frac{1}{2}\mathbf{G}$ 使得 $\varepsilon_{\mathbf{k}}^0 \approx \varepsilon_{\mathbf{k}+\mathbf{G}}^0$）时，简并微扰必须使用。对于两个简并或近简并的平面波 $|\mathbf{k}\rangle$ 和 $|\mathbf{k}+\mathbf{G}\rangle$，简并微扰给出久期方程：
$$ \det\begin{pmatrix} \varepsilon_{\mathbf{k}}^0 - E & V_{\mathbf{G}}^* \\ V_{\mathbf{G}} & \varepsilon_{\mathbf{k}+\mathbf{G}}^0 - E \end{pmatrix} = 0 $$
解得：
$$ E_{\pm} = \frac{1}{2}(\varepsilon_{\mathbf{k}}^0+\varepsilon_{\mathbf{k}+\mathbf{G}}^0) \pm \sqrt{ \left( \frac{\varepsilon_{\mathbf{k}}^0-\varepsilon_{\mathbf{k}+\mathbf{G}}^0}{2} \right)^2 + |V_{\mathbf{G}}|^2 } $$
在边界上 $\varepsilon_{\mathbf{k}}^0 = \varepsilon_{\mathbf{k}+\mathbf{G}}^0$，能隙为 $2|V_{\mathbf{G}}|$。这正是能带形成的物理起源：周期势在布里渊区边界打开能隙。

### 3.3 能带结构与布洛赫态

近自由电子模型给出的能带大致呈抛物线形，但在边界处分裂。例如，一维情况下的能带结构：在 $k=\pm \pi/a$ 处出现能隙，形成第一、第二能带。三维则更为复杂，但基本图像一致。该模型成功解释了单价金属的价电子行为（如 Na 的费米面接近球面），但对过渡金属不适用，因为 $d$ 电子局域性强，需用紧束缚近似。

---

## 第四章 赝势方法

### 4.1 正交平面波与赝势概念

近自由电子近似要求势很弱，但真实离子势在原子核附近很深，不能直接视为微扰。然而，内层电子态与价电子态正交的要求导致有效势大大削弱，这就是**赝势**的思想。

将电子波函数用内层芯态 $\phi_c$ 和价带部分 $\psi_v$ 展开。由于价态与芯态正交，薛定谔方程可化为价态的有效方程：
$$ \left[ -\frac{\hbar^2}{2m}\nabla^2 + V_{\text{ps}}(\mathbf{r}) \right] \psi_v = E \psi_v $$
其中 $V_{\text{ps}}$ 是赝势，它比真实势平坦得多。构造赝势的方法包括正交平面波法（OPW）、模守恒赝势、超软赝势等。

**OPW 方法**：将布洛赫函数写为平面波与芯态投影的线性组合：
$$ |\psi_{\mathbf{k}}\rangle = |\mathbf{k}\rangle - \sum_c |\phi_c\rangle \langle \phi_c | \mathbf{k}\rangle + \sum_{\mathbf{G}} a_{\mathbf{G}} |\mathbf{k}+\mathbf{G}\rangle $$
这种形式自动满足与芯态正交，代入方程导出赝势 $V_{\text{ps}} = V + \sum_c (E - E_c) |\phi_c\rangle \langle \phi_c|$。由于 $E > E_c$，此项为正，抵消了强吸引势，使得赝势很弱。

赝势方法的优势在于可只处理价电子，大大简化计算，是现代电子结构计算（如 DFT）的基础。

---

## 第五章 紧束缚近似

### 5.1 基本思想

当原子间距较大、电子被束缚在原子核附近时，用**紧束缚近似**（TBA）更合适。它将布洛赫函数表示为原子轨道的线性组合：
$$ \psi_{\mathbf{k}}(\mathbf{r}) = \frac{1}{\sqrt{N}} \sum_{\mathbf{R}} e^{i\mathbf{k}\cdot\mathbf{R}} \phi(\mathbf{r} - \mathbf{R}) $$
其中 $\phi(\mathbf{r})$ 是孤立原子的轨道（如 $s,p,d$）。周期性排列后，原子轨道交叠形成能带。

### 5.2 哈密顿矩阵与能带

设孤立原子轨道满足 $H_{\text{at}} \phi(\mathbf{r} - \mathbf{R}) = \varepsilon \phi(\mathbf{r} - \mathbf{R})$。晶体的哈密顿量为 $H = H_{\text{at}} + \Delta V(\mathbf{r})$，$\Delta V$ 是其他原子的势场。在紧束缚近似下，只考虑最近邻或次近邻的交叠积分。计算矩阵元：
$$ \langle \mathbf{R} | H | \mathbf{R}' \rangle = \int \phi^*(\mathbf{r} - \mathbf{R}) H \phi(\mathbf{r} - \mathbf{R}') d^3r $$
对于 $s$ 轨道（非简并），设 $\langle \mathbf{R} | H | \mathbf{R} \rangle = \varepsilon_0$（孤立原子能级加上晶体场修正），$\langle \mathbf{R} | H | \mathbf{R} + \boldsymbol{\delta} \rangle = -t$（跃迁积分，正值 $t>0$ 对应于成键态能量降低）。则布洛赫和为：
$$ E(\mathbf{k}) = \varepsilon_0 - t \sum_{\boldsymbol{\delta}} e^{i\mathbf{k}\cdot\boldsymbol{\delta}} $$
求和遍及最近邻位移 $\boldsymbol{\delta}$。例如，简单立方晶格 $s$ 带：
$$ E(\mathbf{k}) = \varepsilon_0 - 2t(\cos k_x a + \cos k_y a + \cos k_z a) $$
能带宽度为 $12t$。

对于 $p$ 或 $d$ 轨道，需要考虑不同轨道间的交叠积分（Slater-Koster 参数），能带结构更复杂，但原理相同。

### 5.3 紧束缚近似的适用性与推广

紧束缚近似适用于过渡金属（$d$ 电子）、半导体（如硅的 $sp^3$ 杂化轨道）和绝缘体。它可以很好地描述能带的局域性质和拓扑特性（如石墨烯的狄拉克锥源于 $p_z$ 轨道的紧束缚模型）。

---

## 第六章 能带的对称性

### 6.1 空间群与能带分类

能带 $E_n(\mathbf{k})$ 和布洛赫函数 $\psi_{n\mathbf{k}}$ 受到晶体空间群 $G$ 的约束。对于 $g = \{R|\mathbf{t}\} \in G$，有：
$$ g \psi_{n\mathbf{k}}(\mathbf{r}) = \psi_{n\mathbf{k}}(R^{-1}(\mathbf{r}-\mathbf{t})) = e^{-iR\mathbf{k}\cdot\mathbf{t}} \psi_{n, R\mathbf{k}}(\mathbf{r}) $$
这表明 $\psi_{n\mathbf{k}}$ 在 $g$ 作用下变为波矢为 $R\mathbf{k}$ 的本征函数（可能属于同一能带或不同能带）。若 $R\mathbf{k} = \mathbf{k} + \mathbf{G}$，则能带在 $\mathbf{k}$ 处具有小群 $P_{\mathbf{k}}$ 的对称性。小群的不可约表示可标记能带在 $\mathbf{k}$ 点处的简并度和变换性质。

### 6.2 表示与能带交叉

在小群表示维数大于1的高对称点或线上，能带简并是受对称性保护的。例如，石墨烯的 $K$ 点具有 $D_3$ 小群，产生二维不可约表示，导致狄拉克锥。若小群表示是一维的，则能带交叉一般会被避免（避免交叉规则），除非有额外的对称性（如时间反演、手征对称）。

**相容性关系**：当从高对称点沿着对称线移动时，不可约表示分解为低对称群的表示，从而决定能带的走向和简并度的解除。这对于理解能带结构拓扑分类至关重要。

### 6.3 时间反演对称性

时间反演算符 $T$（反幺正）满足 $T^2 = \pm 1$（对整数自旋为 $+1$，半整数自旋为 $-1$）。在无外磁场且无磁序时，$[T, H]=0$，导致 $E_n(\mathbf{k}) = E_n(-\mathbf{k})$（对于非自旋极化系统，还要求 $E_n(\mathbf{k}) = E_n(-\mathbf{k})$ 且可能出现 Kramers 简并）。时间反演与点群组合形成磁点群，用于磁性晶体的分类。

---

## 第七章 能态密度与费米面

### 7.1 能态密度

电子态密度 $g(E)$ 定义为能量在 $[E, E+dE]$ 之间的电子态数目：
$$ g(E) = \frac{V}{(2\pi)^3} \sum_n \int_{BZ} \delta(E - E_n(\mathbf{k})) \, d^3k $$
它是电子热力学和输运的核心量。在自由电子气中，$g(E) \propto E^{1/2}$；在能带边缘（如导带底），通常可近似为抛物线：$E(\mathbf{k}) = E_c + \frac{\hbar^2 k^2}{2m^*}$，此时 $g(E) \propto (E-E_c)^{1/2}$，$m^*$ 为有效质量。当维度降低时，态密度表现出不同的奇异性（如二维为台阶，一维为反平方奇异点）。

**范霍夫奇点**：在能带具有驻点（$\nabla_{\mathbf{k}} E_n(\mathbf{k}) = 0$）的能量位置，$g(E)$ 出现奇异性。三维中为平方根或对数奇点，二维为对数奇点，一维为反平方奇点。

### 7.2 费米面

在绝对零度，电子占据 $E \le E_F$ 的所有态，$E_F$ 为费米能级。在 $\mathbf{k}$ 空间中，等能面 $E_n(\mathbf{k}) = E_F$ 称为**费米面**。费米面的几何形状决定了金属的几乎所有低能性质：电导、热导、霍尔效应、磁阻、de Haas-van Alphen 效应等。

对于简单金属（如 Na），费米面接近球面，但在布里渊区边界略有畸变。对于贵金属（Cu、Ag、Au），费米面与布里渊区边界接触形成“颈状”结构。对于过渡金属，费米面复杂，包含电子型和空穴型口袋。费米面的测量主要通过 de Haas-van Alphen 效应（磁化率随磁场的量子振荡）和角分辨光电子能谱（ARPES）。

### 7.3 态密度与费米面的关系

在费米面附近，态密度 $g(E_F)$ 决定了电子比热系数 $\gamma = \frac{\pi^2}{3} k_B^2 g(E_F)$。费米波矢 $k_F$ 与电子密度 $n$ 的关系通过 Luttinger 定理：在相互作用下，费米面包围的体积仍由电子密度决定（即在 $\mathbf{k}$ 空间中，费米面内的体积等于 $(2\pi)^3$ 乘以电子密度除以自旋简并度）。

---

## 第八章 方法比较与近代发展

### 8.1 近自由与紧束缚：对偶性

近自由电子近似（弱周期势）和紧束缚近似（弱交叠）是能带理论的两个极端。事实上，它们通过傅里叶变换互为对偶：在 $\mathbf{k}$ 空间展开的平面波基与在实空间的局域轨道基互为傅里叶共轭。现代第一性原理能带计算（如 DFT）通常使用混合基组（如平面波加赝势、线性缀加平面波 LAPW 等），兼具两者优点。

### 8.2 能带拓扑

近二十年来，能带的拓扑性质成为凝聚态物理的前沿。通过贝里曲率、陈数、$\mathbb{Z}_2$ 不变量等，能带被分类为拓扑绝缘体、狄拉克半金属、韦尔半金属等。这些拓扑性质源于能带在布里渊区中的整体结构，与本讲涉及的对称性、能带交叉等密切相关。例如，石墨烯的低能狄拉克锥是能带对称性与手征对称性共同作用的结果。

---

## 第九章 总结

能带理论是固体物理学的核心范式。我们从布洛赫定理出发，建立了周期势中电子态的基本框架。近自由电子近似揭示了能隙的起源，赝势方法为实际计算铺平了道路，紧束缚近似则抓住了局域电子的本质。能带的对称性由空间群小群刻画，态密度和费米面则直接连接实验可测的物理量。无论是传统的半导体、金属物理，还是现代的拓扑物态，能带理论始终是理解电子性质的基石。

---

## 参考文献

1.  Ashcroft, N. W., & Mermin, N. D. (1976). *Solid State Physics*. Holt, Rinehart and Winston. （第8-12章）
2.  Kittel, C. (2005). *Introduction to Solid State Physics* (8th ed.). Wiley. （第6-9章）
3.  Harrison, W. A. (1989). *Electronic Structure and the Properties of Solids*. Dover.
4.  Heine, V. (1970). "The Pseudopotential Concept", in *Solid State Physics*, Vol. 24, p.1.
5.  Slater, J. C., & Koster, G. F. (1954). "Simplified LCAO Method for the Periodic Potential", *Phys. Rev.* 94, 1498.
6.  Dresselhaus, M. S., Dresselhaus, G., & Jorio, A. (2008). *Group Theory: Application to the Physics of Condensed Matter*. Springer. （第9-10章）
7.  Nakahara, M. (2003). *Geometry, Topology, and Physics* (2nd ed.). Taylor & Francis. （第1章：群论基础）
8.  Hasan, M. Z., & Kane, C. L. (2010). "Colloquium: Topological insulators", *Rev. Mod. Phys.* 82, 3045.

你总结的这五点非常精彩，精准抓住了能带理论中从“分析”到“几何”再到“拓扑”的跃迁。这正是Nakahara式视角的精髓——将固体物理的能带结构视为一个微分流形（环面）上的函数与向量丛，并由此揭示其内禀的几何与拓扑不变量。

顺着这个思路，我以同样的风格，为你把这段论述扩展成一个完整的小节，可以作为之前《电子的动力学》那讲的“几何化总结”或独立阅读材料。

---

# 从色散关系到拓扑物态

## 1. 能带作为环面上的层 (Bands as Sheets over a Torus)

固体物理的核心是周期势场中的单电子问题。布洛赫定理将问题约化到第一布里渊区（BZ），而周期边界条件（玻恩-冯·卡门）使波矢 $\mathbf{k}$ 离散化，但在热力学极限下，$\mathbf{k}$ 成为连续变量，且由于 $\mathbf{k} \sim \mathbf{k}+\mathbf{G}$，布里渊区具有**环面 $T^n$** 的拓扑结构（$n=1,2,3$）。

对于每个能带指标 $m$，色散关系 $\epsilon_m: T^n \to \mathbb{R}$ 是一个定义在环面上的光滑函数（除简并点外）。因此，**第 $m$ 条能带是嵌入在 $(n+1)$ 维空间 $T^n \times \mathbb{R}$ 中的一个超曲面**。多条能带则是一叠这样的超曲面，它们在动量空间中可能相交（简并）或由能隙隔开。

这个几何图像立即带来两个洞察：
- **周期边界**：函数 $\epsilon_m(\mathbf{k})$ 并非任意，必须满足 $T^n$ 上的周期性条件，这对其临界点的分布（莫尔斯理论）施加了全局约束。
- **多层结构**：能带之间由“间隙”分隔，而间隙的拓扑性质（如陈数、$\mathbb{Z}_2$ 不变量）决定了是否有受保护的边缘态。

## 2. 群速度与动量空间度规

色散关系的一阶导数给出了电子的半经典动力学核心——**群速度**：
$$ \mathbf{v}_m(\mathbf{k}) = \frac{1}{\hbar} \nabla_{\mathbf{k}} \epsilon_m(\mathbf{k}). $$
从微分几何看，$\nabla_{\mathbf{k}} \epsilon_m$ 是能带超曲面上某点的**切平面法向**在动量空间中的投影。而等能面 $\{ \mathbf{k} \mid \epsilon_m(\mathbf{k}) = E \}$ 的**法向量**正是 $\nabla_{\mathbf{k}} \epsilon_m$，因此群速度始终垂直于等能面。

二阶导数给出了**有效质量张量**：
$$ \left( \frac{1}{m^*} \right)_{ij} = \frac{1}{\hbar^2} \frac{\partial^2 \epsilon_m}{\partial k_i \partial k_j}. $$
这个黑塞矩阵定义了动量空间中的一个**度规**：在能带极值附近，能量可以近似为 $\epsilon \approx \epsilon_0 + \frac{1}{2} \sum_{ij} (m^*)^{-1}_{ij} (k_i - k_{i0})(k_j - k_{j0})$。有效质量张量的特征值反映了能带在不同方向上的“弯曲程度”：能带越平（曲率小），有效质量越大，电子越难加速。这一几何直观对于理解半导体中的载流子输运至关重要。

## 3. 费米面：动量空间中的子流形

对于金属，基态由费米能级 $E_F$ 以下的态填满。费米面定义为：
$$ \Sigma_F = \{ \mathbf{k} \in T^n \mid \epsilon_m(\mathbf{k}) = E_F \}. $$
在三维动量空间中，费米面通常是一个二维闭合曲面（余维数为1）。它的几何形状——球形、立方体、圆柱形、或“赝能隙”下的复杂网络——直接反映了晶格对称性和能带填充情况。

费米面的拓扑结构（如连通性、亏格、是否包含电子型或空穴型口袋）决定了金属的输运、磁性及集体激发（如嵌套、Kohn反常）。当通过掺杂或压力使费米面拓扑发生变化（例如一个口袋消失或两个口袋合并）时，系统会发生**Lifshitz相变**，这本质上是等能面子流形的拓扑突变。

## 4. 临界点与范霍夫奇点：莫尔斯理论的应用

能带函数 $\epsilon_m(\mathbf{k})$ 在环面上的梯度为零的点称为**临界点**（驻点）——包括极小值、极大值和鞍点。根据**莫尔斯理论**，一个定义在紧致流形（如环面 $T^n$）上的莫尔斯函数（非退化临界点）必须满足一定的数量关系。例如，二维环面上，极小点、鞍点和极大点的个数满足 $N_{\min} - N_{\text{saddle}} + N_{\max} = 0$（欧拉示性数为0）。这解释了为何能带必然出现鞍点。

在临界点附近，$\nabla_{\mathbf{k}} \epsilon = 0$，导致态密度公式：
$$ g(E) = \frac{V}{(2\pi)^n} \sum_m \oint_{\epsilon_m(\mathbf{k})=E} \frac{dS}{|\nabla_{\mathbf{k}} \epsilon_m|} $$
出现奇异性——**范霍夫奇点**。在二维中，鞍点产生对数发散；在一维中，带边产生 $1/\sqrt{E}$ 发散；在三维中，鞍点产生可积的平方根奇点。这些奇点深刻影响了电子比热、磁化率等物理量，是能带几何直接反映在热力学中的体现。

## 5. 拓扑能带论：贝里曲率与陈数

上述讨论仅涉及能带函数 $\epsilon_m(\mathbf{k})$ 的**分析性质**（梯度、曲率、临界点）。然而，完全描述一个布洛赫电子态还需要其本征矢 $|u_{m\mathbf{k}}\rangle$。在能带不简并且与其它能带由能隙隔开的区域，$|u_{m\mathbf{k}}\rangle$ 定义了环面 $T^n$ 到复射影空间的一个映射。该映射的几何性质由**贝里联络** $\mathcal{A}_m(\mathbf{k}) = i \langle u_{m\mathbf{k}} | \nabla_{\mathbf{k}} u_{m\mathbf{k}} \rangle$ 及其曲率 $\Omega_m(\mathbf{k}) = \nabla_{\mathbf{k}} \times \mathcal{A}_m(\mathbf{k})$ 描述。

在二维系统中，贝里曲率的积分给出**陈数**：
$$ C_m = \frac{1}{2\pi} \int_{T^2} \Omega_m(\mathbf{k}) \cdot d^2\mathbf{k}. $$
陈数是一个拓扑不变量——只要能隙不闭合，它就不随参数连续变化。当 $C_m \neq 0$ 时，系统是**陈绝缘体**，表现出量子化的霍尔电导 $\sigma_{xy} = -\frac{e^2}{h} \sum_{m \text{ occ}} C_m$。

更一般地，在时间反演对称性下，二维拓扑绝缘体由 $\mathbb{Z}_2$ 不变量分类；三维拓扑绝缘体由四个 $\mathbb{Z}_2$ 不变量 $(\nu_0; \nu_1\nu_2\nu_3)$ 分类。这些拓扑量子数不仅依赖于能带的色散关系（即能量的分布），更依赖于本征态在整个布里渊区上的**整体缠绕**方式。即使在 $\epsilon_m(\mathbf{k})$ 看起来平淡无奇（如平带）的情况下，也可能隐藏着非平凡的拓扑序。

**小结**：将能带视为环面上的函数与向量丛，我们得以区分“几何”（群速度、有效质量、临界点）与“拓扑”（陈数、$\mathbb{Z}_2$ 不变量）。前者由色散关系的局域导数决定，后者由本征态的整体结构决定。这一视角不仅统一了固体物理中的许多经典概念（费米面、范霍夫奇点、有效质量），更催生了拓扑绝缘体、狄拉克/外尔半金属等现代前沿。正如 Nakahara 所言：“物理定律的深刻之处，往往隐藏在底流形的拓扑之中。”
