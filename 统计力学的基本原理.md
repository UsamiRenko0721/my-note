---
up:
  - "[[笔记]]"
related:
date: 2026-03-08
---

# 基础概念

要理解系综理论，我们首先要建立两个基础概念：

>[!important] 相空间与代表点
>对于一个拥有 $N$ 个粒子的经典系统，其微观状态可以用 $3N$ 个广义坐标 $q_i$ 和 $3N$ 个广义动量 $p_i$ 来完整表征。在几何上，我们可以将这 $6N$ 个变量看作 $6N$ 维“相空间”中的一个点，称为**代表点**。随着时间推移，代表点会在相空间中刻画出一条运动轨道。

>[!important] 系综
>为了避免追踪单一系统极其复杂的演化轨迹，吉布斯等人引入了系综的概念。系综是指由大量（理论上是无数个）处于相同宏观约束（如相同的 $N, V, E$）下的**全同、独立系统的集合**，这些系统被称为原系统的“思维复本”（mental copies）。在任意时刻 $t$，这些复本分布在相空间中所有可能的微观态上。

除此之外，还有个本来不应该属于统计力学的一个定理，本来是理论力学的

>[!important] 刘维尔定理
>在相空间中沿着系统轨道运动的局部密度随时间保持恒定，这意味着这群代表点在相空间中的运动就像不可压缩流体一样。
>$$\frac{d\rho}{dt} = 0 $$




# 理论公设

系综理论建立在以下两个基础公设之上：

1. **等先验概率公设**：对于一个给定的宏观态，系统处于其任何一个可及（accessible）微观态的概率都是相等的


2. **无规相位假设**： 这是针对量子统计的补充公设。它假设系综中的复本是基矢状态的非相干叠加，确保了成员系统之间没有相互干涉或关联，从而我们可以独立地研究每一个复本


# 重要结论

通过系综理论，我们得到了统计力学中最具革命性的结论

1. **玻尔兹曼熵公式**： 最重要的公式莫过于玻尔兹曼熵公式：$S = k \ln \Omega$。它通过宏观态对应的微观态总数 $\Omega$ 确定了熵 $S$ 的绝对值，为热力学第三定律提供了理论基础

2. **各态历经假说**： 理论证明（在满足一定条件下），一个物理量的长时间平均值等于它在系综中的平均值。这意味着我们可以通过计算系综平均来预言实验测量得到的期望值

3. **三大经典分布**：
    
    - **微正则分布**：孤立系统的概率分布。
    - **正则分布**：与大热库平衡的系统分布，$P_r \propto \exp(-\beta E_r)$。
    - **巨正则分布**：允许粒子数涨落的系统分布，$P_{r,s} \propto \exp(-\alpha N_r - \beta E_s)$。

ps. 这三个分布的意思是：在给定宏观约束下，一整个宏观系统处于某个特定态的概率是多少。

# 一些推导证明

## 玻尔兹曼熵公式

首先，我们考虑两个分别处于平衡态的独立物理系统 $A_1$ 和 $A_2$

- 系统 $A_1$ 的宏观态由 $(N_1, V_1, E_1)$ 表征，其对应的微观态数为 $\Omega_1(N_1, V_1, E_1)$
- 系统 $A_2$ 的宏观态由 $(N_2, V_2, E_2)$ 表征，其对应的微观态数为 $\Omega_2(N_2, V_2, E_2)$

现在将这两个系统进行热接触，允许它们交换能量，但保持各自的体积和粒子数不变。此时，复合系统 $A^{(0)} = A_1 + A_2$ 的总能量 $E^{(0)} = E_1 + E_2$ 是一个恒定量。由于两个系统是相互独立的，复合系统 $A^{(0)}$ 处于特定能量分配 $(E_1, E_2)$ 下的微观态总数 $\Omega^{(0)}$ 是子系统微观态数的乘积： $$\Omega^{(0)}(E_1, E_2) = \Omega_1(E_1) \cdot \Omega_2(E_2) = \Omega_1(E_1) \cdot \Omega_2(E^{(0)} - E_1)$$*物理系统如果听其自然，会沿着使其配容数（微观态数）不断增加的方向演化，直到达到可能的最大值，这样的平衡状态被称为“最概然状态”，即对应微观态数 $\Omega^{(0)}$ 最大的状态。* 为了寻找这个极大值，我们对 $E_1$ 求导并令其为零： $$\left( \frac{\partial \Omega^{(0)}}{\partial E_1} \right) = \left( \frac{\partial \Omega_1}{\partial E_1} \right) \Omega_2 + \Omega_1 \left( \frac{\partial \Omega_2}{\partial E_2} \right) \frac{\partial E_2}{\partial E_1} = 0$$由于 $\frac{\partial E_2}{\partial E_1} = -1$，上述条件可以转化为对数形式： $$\left( \frac{\partial \ln \Omega_1}{\partial E_1} \right)_{E_1=\bar{E}_1} = \left( \frac{\partial \ln \Omega_2}{\partial E_2} \right)_{E_2=\bar{E}_2}$$于是不妨定义一个统计量 $\beta$ : $$\beta \equiv \left( \frac{\partial \ln \Omega}{\partial E} \right)_{N, V}$$当两个系统达到热平衡时，它们的 $\beta$ 参数必须相等（$\beta_1 = \beta_2$）。而在热力学中，我们知道热平衡意味着两个系统的温度相等（$T_1 = T_2$）。因此，$\beta$ 必然是热力学温度 $T$ 的某种函数。

回顾热力学公式，熵 $S$ 的定义满足： $$\left( \frac{\partial S}{\partial E} \right)_{N, V} = \frac{1}{T}$$对比统计学定义的 $\beta$ 和热力学的 $1/T$，可以推断出 $\beta$ 与 $1/T$ 成正比，即： $$\frac{\Delta S}{\Delta (\ln \Omega)} = \frac{1}{\beta T} = \text{常数}$$根据上述对应关系，玻尔兹曼建立了熵与微观态数的对数关系，而普朗克首先写出了不带附加常数的明确公式： $$S = k \ln \Omega$$这里比例常数 $k$ 被称为玻尔兹曼常数。通过这一定义，熵具有了加和性（源于 $\Omega$ 的相乘性），并且为热力学第三定律提供了理论基础：当系统处于唯一的微观态（$\Omega=1$）时，其熵为零。

总结来说，核心逻辑是将“**达到平衡态**”等同于“**微观态数最大化**”


## 各态历经假说

所谓的**各态历经系统**，其定义是：如果时间不受限制，一个未受扰动的物理系统的运动轨道，最终总将通过与该系统固定能量 $E$ 相容的每一个相点（或者至少是该相点的邻近区域）。

简单来说，这意味着单个物理系统在漫长的时间演化中，会“不知疲倦”地走遍所有在宏观约束下它被允许进入的微观状态。

在实际物理测量中，我们得到的是某个物理量 $f$ 的“长时间平均值”（用 $\bar{f}$ 表示）而在理论计算中，我们计算的是该量在某一时刻在整个系综上的“系综平均值”（用 $\langle f \rangle$ 表示）

1. 对于一个处于平衡态的物理系统，我们用“定态系综”来描述它，这意味着其密度函数 $\rho$ 不随时间改变，从而任何物理量的系综平均值 $\langle f \rangle$ 必然是与时间无关的常数
2. 由于 $\langle f \rangle$ 本身不随时间变化，那么对 $\langle f \rangle$ 再取一次时间平均，结果依然是 $\langle f \rangle$$$\braket{ f } = \braket{ f } \text{ 的时间平均} = ( f \text{ 的系综平均} ) \text{ 的时间平均} $$
3. 计算“系综平均的时间平均”时，由于这两个求平均的过程在数学上是完全独立的，其操作顺序可以颠倒。因此$$\langle f \rangle = \text{（} f \text{ 的长时间平均）的系综平均}$$
4. 根据各态历经假说，由于单个系统会遍历所有微观态，那么物理量 $f$ 的长时间平均值，对于系综中的每一个成员系统来说都必然是相同的，且不取决于它们的初态如何。
5. 既然系综中每个复本的时间平均值都相等，那么它们的系综平均值也就等于其中任意一个复本的时间平均值，即 $$\langle f \rangle = \bar{f}$$
这就是我们常说的：**系综平均等于长时间平均**。

## 微正则分布

在经典统计力学中，系统的微观状态由相空间中的代表点表征。对于一个能量精确为 $E$ 的孤立系统，其代表点在相空间中的轨迹被严格限制在一个能量超曲面 $H(q, p) = E$ 上。

然而，从物理现实和数学处理的角度来看，规定一个绝对精确的能量值是不合理的。因此，我们通常设定一个极窄的能量范围 $[E - \frac{1}{2}\Delta, E + \frac{1}{2}\Delta]$，其中 $\Delta \ll E$。在相空间中，这对应于一个厚度极薄的能量壳层

基于等先验概率公设，我们定义**相空间中的密度函数** $\rho(q, p)$。这个函数描述了系综内的代表点在相空间中的分布情况。对于微正则系综，分布函数满足以下形式
- 在能量超壳体内（即 $E - \frac{1}{2}\Delta \leq H(q, p) \leq E + \frac{1}{2}\Delta$） $$\rho(q, p) = \text{常数}$$
- 在能量超壳体之外 $$\rho(q, p) = 0$$
这意味着在允许的能量范围内，代表点在相空间中是均匀分布的。在给定的体积元 $d\omega$ 内找到代表点的概率，仅取决于该体积元的大小，而与其位置无关。

如果我们站在量子力学的视角下，微观态是不连续的能级。此时，微正则分布通过密度矩阵 $\rho_{mn}$ 来表征。在能量表象中，密度矩阵是一个对角矩阵，其矩阵元定义如下：
- 对于每一个可及态（即能量处于允许范围内的态）： $$\rho_{nn} = \frac{1}{\Gamma}$$
- 对于所有其他状态： $$\rho_{nn} = 0$$这里 $\Gamma$ 是系统在给定宏观条件下可能具有的不同微观态总数。这个表达式再次体现了所有可及态出现概率均等（即 $1/\Gamma$）的核心思想。


## 正则分布

### 方法一：系统与大热库间的热平衡

想象一个小系统 $A$ 浸没在一个巨大的热库 $A'$ 中。两者共同组成一个孤立的复合系统 $A^{(0)} = A + A'$。其总能量为 $E^{(0)} = E_r + E_r' = \text{常数}$，其中 $E_r$ 是小系统 $A$ 处于状态 $r$ 时的能量

根据等概率原理，复合系统 $A^{(0)}$ 处于其任何一个微观态的概率都是相等的。因此，系统 $A$ 处于能量为 $E_r$ 的特定状态的概率 $P_r$，正比于此时热库 $A'$ 能够具有的微观状态数 $\Omega'(E_r')$ 

$$P_{r} \propto \Omega'(E^{(0)}-E_{r}) $$

考虑道 $E_{r}\ll E^{(0)}$ 所以可以考虑对 $E_{r}$ 在 $E^{(0)}$ 附近展开
$$\ln \Omega'(E^{(0)}-E_{r}) = \ln \Omega'(E^{(0)}) + \left( \frac{\partial \ln \Omega'}{\partial E'} \right)_{E'=E^{(0)}}(-E_{r}) + \dots $$
就像在微正则系综中一样，我们定义
$$\beta = \left( \frac{\partial \ln \Omega}{\partial E} \right)_{N,V} $$
于是
$$\ln \Omega'(E^{(0)}-E_{r}) = \text{Const} - \beta E $$
$$P_{r} \propto \exp(-\beta E_{r}) $$
最后归一化条件要求 $\sum_{r}P_{r}=1$ 所以就得到了正则分布
$$P_{r} = \frac{\exp(-\beta E_{r})}{\displaystyle\sum_{r}\exp(-\beta E_{r})} $$
并将分母定义为配分函数
$$Z_{N}(V,T) = \sum_{r} \exp(-\beta E_{r}) $$


### 方法二：系综的最概然值方法

我们考虑一个由 $\mathcal{N}$ 个全同且独立的系统（即“思维复本”）组成的巨大系综。这些系统共同分享一个总能量 $\mathcal{E} = \mathcal{N}U$，其中 $U$ 是单个系统的平均能量。 设 $n_r$ 为系综中处于能量为 $E_r$ 状态的系统数目。那么整个系综必须满足以下两个基本的宏观约束条件：

$$\sum_r n_r = \mathcal{N} ,\quad \sum_r n_r E_r = \mathcal{E} = \mathcal{N}U $$

根据等概率公设，系综处于任何一种可能的微观态的概率都是相等的。因此，实现某种特定分布集合 ${n_r}$ 的方式数（即统计权重 $W$）越多，这种分布出现的概率就越大。 对于系综而言，将 $\mathcal{N}$ 个系统分配到不同能级上的组合方式数为： $$W\{n_r\} = \frac{\mathcal{N}!}{\prod_r n_r!}$$当系综成员数 $\mathcal{N} \to \infty$ 时，系综的行为由使 $W$ 取极大值的分布 ${n_r^*}$ 决定，这就是**最概然分布**。 为了数学处理方便，我们对 $W$ 取对数并应用斯特林公式（$\ln n! \approx n \ln n - n$）： $$\ln W \approx \mathcal{N} \ln \mathcal{N} - \sum_r n_r \ln n_r$$ 对上式取变分 $\delta \ln W = 0$，并结合前面提到的两个约束条件，引入拉格朗日乘子 $\alpha$ 和 $\beta$： $$\sum_r [ -(\ln n_r + 1) - \alpha - \beta E_r ] \delta n_r = 0$$由于变分 $\delta n_r$ 是任意的，括号内的项必须恒等于零，从而得到最概然分布下能级 $r$ 的系统数目 $n_r$$$n_r \propto \exp(-\beta E_r)$$由于我们要找的是系综中任一系统处于状态 $r$ 的概率 $P_r$，它等于 $n_r^*/\mathcal{N}$ 利用 $\sum P_r = 1$ 的条件我们最终得到了正则分布的标准形式$$P_r = \frac{\exp(-\beta E_r)}{\sum_r \exp(-\beta E_r)}$$ 分母 $\sum_r \exp(-\beta E_r)$ 即为配分函数
$$Z_{N}(V,T) = \sum_{r}\exp(-\beta E_{r}) $$
通过将此统计结果与热力学基本定律（如亥姆霍兹自由能 $A = U - TS$）进行类比，我们可以确定参数 $\beta$ 的物理意义： $$\beta = \frac{1}{kT}$$
### 方法三：鞍点法

参考 [[鞍点法推导分布]] 



## 巨正则分布

### 方法一：系统与粒子-能量库间的平衡（物理直觉法）

这是最直观的推导方式，我们将研究对象视为浸没在一个巨大的“库”中的子系统

设系统 $A$ 与一个巨大的粒子-能量库 $A'$ 接触

- 系统 $A$ 处于由粒子数 $N_r$ 和能量 $E_s$ 表征的状态
- 两者组成孤立的复合系统 $A^{(0)}$，总粒子数 $N^{(0)} = N_r + N_r'$ 和总能量 $E^{(0)} = E_s + E_s'$ 均为常数

系统 $A$ 处于特定 $(N_r, E_s)$ 状态的概率 $P_{r,s}$ 直接正比于此时库 $A'$ 能够具有的微观状态数 $\Omega'(N_r', E_s')$
$$P_{r,s} \propto \Omega'(N^{(0)} - N_r, E^{(0)} - E_s)$$

由于库远大于系统（$N_r \ll N^{(0)}, E_s \ll E^{(0)}$），我们对 $\ln \Omega'$ 进行多元泰勒展开 $$\ln \Omega'(N^{(0)} - N_r, E^{(0)} - E_s) \approx \ln \Omega'(N^{(0)}, E^{(0)}) + \left(\frac{\partial \ln \Omega'}{\partial N'}\right) (-N_r) + \left(\frac{\partial \ln \Omega'}{\partial E'}\right) (-E_s)$$类似的定义
$$\begin{gather}
\left( \frac{\partial \ln \Omega}{\partial E} \right)_{V,N} = \beta \\
\left( \frac{\partial \ln \Omega}{\partial N} \right)_{V,E} = \alpha
\end{gather} $$
其中 $\beta$ 和微正则系综一样是反温度 $\frac{1}{kT}$ 关注 $\alpha$ 对应热力学的什么参数，由于玻尔兹曼熵公式 $S=k\ln \Omega$ 于是考虑到 $dS=\frac{1}{T}dE + \frac{P}{T}dV - \frac{\mu}{T}dN$ 有
$$\alpha = \left( \frac{\partial \ln \Omega}{\partial N} \right)_{V,E} = \frac{1}{k}\left( \frac{\partial S}{\partial N} \right)_{V,E} = -\frac{\mu}{kT} $$
将这些代回得到
$$P_{r,s} \propto \exp( - \beta E_{s} - \alpha N_{r} )$$
将概率归一化得到
$$P_{r,s} = \frac{\exp(-\beta E_{s} - \alpha N_{r})}{\displaystyle\sum_{r,s}\exp(-\beta E_{s}-\alpha N_{r})} $$
分母被定义为巨配分函数
$$\Xi(V,T,\mu) = \sum_{r,s} \exp(-\beta E_{s} - \alpha N_{r})  $$

### 方法二：巨正则系综的最概然值方法

这种方法不依赖于具体的“库”，而是将整个系综看作一个整体进行统计分配。

考虑由 $\mathcal{N}$ 个全同系统组成的系综，总粒子数为 $\mathcal{N}\bar{N}$，总能量为 $\mathcal{N}\bar{E}$。令 $n_{r,s}$ 表示具有粒子数 $N_r$ 和能量 $E_s$ 的系统数目，需满足

$$\sum n_{r,s} = \mathcal{N} ,\quad \sum n_{r,s}N_{r} = \mathcal{N}\bar{N} ,\quad \sum n_{r,s}E_{s} = \mathcal{N}\bar{E} $$

对于实现分布集合为 $\{ n_{r,s} \}$ 的方式数 $W\{ n_{r,s} \}$ 为
$$W\{ n_{r,s} \} = \frac{\mathcal{N}!}{\prod n_{r,s}!} $$
$$\begin{align}
\ln W & = \ln \mathcal{N}! - \sum \ln (n_{r,s}!) \approx \mathcal{N}\ln \mathcal{N} - \mathcal{N} - \sum( n_{r,s}\ln n_{r,s} - n_{r,s} ) \\ & =\mathcal{N}\ln \mathcal{N} - \sum n_{r,s}\ln n_{r,s}
\end{align} $$
现在要求 $W$ 在满足那三个约束下最大化，使用拉格朗日乘子法
$$L = \ln W - \gamma\left( \sum n_{r,s} - \mathcal{N} \right) - \alpha\left( \sum n_{r,s}N_{r} - \mathcal{N}\bar{N} \right) - \beta\left( \sum n_{r,s}E_{s} - \mathcal{N}\bar{E} \right) $$
所以得到方程 (我标了 $*$ 以说明它是最概然状态的分布，也就是满足拉格朗日乘子法的解)
$$-(\ln n_{r,s}^{*} + 1) - \gamma - \alpha N_{r} - \beta E_{s} = 0 $$
$$n_{r,s}^{*} \propto \exp(-\beta E_{s} - \alpha N_{r}) $$
考虑到归一化条件有
$$n_{r,s}^{*} = \frac{\exp(-\beta E_{s}-\alpha N_{r})}{\displaystyle\sum_{r,s}\exp(-\beta E_{s}-\alpha N_{r})}\mathcal{N} $$
将分母定义为某个函数
$$\Xi = \sum_{r,s} \exp(-\beta E_{s} - \alpha N_{r}) $$
并定义 q-势
$$q= \ln\Xi = \ln\left( \sum_{r,s} \exp(-\beta E_{s} - \alpha N_{r}) \right) $$
$$dq = -\bar{N}d\alpha - \bar{E}d\beta - \sum_{r,s} P_{r,s}\beta dE_{s} $$
$$d(q+\alpha \bar{N} + \beta \bar{E}) = \alpha d\bar{N} + \beta d\bar{E} - \sum_{r,s}P_{r,s}\beta dE_{s} $$
回忆热力学第一定律的微分形式
$$\frac{dQ}{kT} = \frac{1}{kT}d\bar{E} + \frac{P}{kT}dV - \frac{\mu}{kT}d\bar{N} $$
