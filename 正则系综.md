---
up:
  - "[[写题]]"
related:
date: 2026-02-07
---
# 定义与物理图景

- **系统描述**：系统 $A$ 与一个巨大的热库 $A'$ 接触，达到热平衡
- **约束条件**：粒子数 $N$、体积 $V$ 和温度 $T$ 固定（而能量 $E$ 是可以波动的）

由于系统与热库可以交换能量，系统不再处于单一的能量面上，而是以一定的概率分布在所有可能的能量态上

# 玻尔兹曼分布推导

将系统 $A$ 和热库 $A'$ 视为一个孤立的复合系统 $A^{(0)}$，应用微正则系综的等概率原理，系统处于能量为 $E_r$ 的微观态的概率 $P_r \propto \Omega'(E^{(0)} - E_r)$。对熵 $S' = k \ln \Omega'$ 在 $E_r \ll E^{(0)}$ 处进行展开：
$$\ln \Omega'(E^{(0)}-E_{r}) \approx \ln \Omega'(E^{(0)}) - \left( \frac{\partial \ln \Omega'(E)}{\partial E} \right)E_{r} = const - \beta E_{r} $$
所以
$$P_{r} = Ne^{ -\beta E_{r} } $$
考虑到归一化，所以归一化系数 $N = \left( \sum_{r} e^{ -\beta E_{r} } \right)^{-1}$ 所以
$$P_{r} = \displaystyle\frac{e^{ -\beta E_{r} }}{\displaystyle\sum_{r}e^{ -\beta E_{r} }} $$
这就是玻尔兹曼分布

# 配分函数定义

定义
$$Z_{N}(V,T) = \sum_{r} e^{ -\beta E_{r} } $$
若是连续的形式那么
$$Z_{N}(V,T) = \frac{1}{N!h^{3N}} \int e^{ -\beta H(q,p) } d\Gamma $$
*注：$1/N!$ 用于修正全同粒子，避免吉布斯佯谬。*


# 热力学量的统计表示

所有的热学量可以通过配分函数来表示

$$U = \frac{\sum E_{r}e^{ -\beta E_{r} }}{\sum e^{ -\beta E_{r} }} = - \frac{\partial }{\partial \beta}\ln\left(  \sum_{r}\exp(-\beta E_{r})  \right) = -\frac{\partial \ln Z}{\partial \beta} $$
一个极其重要的事情是，亥姆霍兹自由能，它被定义为
$$A = U-TS , \quad dA = -SdT - PdV + \mu dN $$
于是
$$U = A +TS = A - T\left( \frac{\partial A}{\partial T} \right)_{N,V} =  - T^{2}\left[  \frac{\partial }{\partial T}\left( \frac{\partial A}{\partial T} \right)  \right]_{N,V} = \left[  \frac{\partial (A / T)}{\partial\left( 1 / T \right)}  \right]_{N,V} $$
所以对应关系显然有
$$\beta = \frac{1}{kT} ,\quad \ln\left( \sum_{r}\exp(-\beta E_{r}) \right) = -\frac{A}{kT} $$
换言之
$$A = -kT\ln Z $$
故
$$S = -\left( \frac{\partial A}{\partial T} \right)_{N,V} = k\ln Z + kT \left( \frac{\partial \ln Z}{\partial T} \right)_{N,V} $$
$$P = -\left( \frac{\partial A}{\partial V} \right)_{N,T} = kT \left( \frac{\partial \ln Z}{\partial V} \right)_{N,T} $$

# 能量涨落现象

考虑能量的均方差
$$\frac{\partial U}{\partial \beta} = - \frac{\sum E_{r}^{2}e^{ -\beta E_{r} }}{Z} + \frac{\left( \sum E_{r}e^{ -\beta E_{r} } \right)^{2}}{Z^{2}} = -\braket{  E^{2}  } + \braket{  E  }^{2}   $$
于是
$$\braket{  (\Delta E)^{2}  } = -\frac{\partial U}{\partial \beta} = kT^{2} \frac{\partial U}{\partial T} = kT^{2}C_{V} $$
所以
$$ \frac{\sqrt{ \braket{  (\Delta E)^{2}  } }}{\braket{  E  } } = \frac{\sqrt{ kT^{2}C_{V} }}{U} \sim \mathcal{O}(N^{-1/2}) $$
因此，对于大 $N$ 情形， $E$ 值的相对方均根涨落是完全可以忽略不计的。这样，就所有情况而论，正则系综里的一个系统就具有等千或几乎等千平均能量 $U$ 的能量；因此，在正则系综中的情况与在微正则系综中的情况简直是相同的，这就解释了为什么两种系综会导出几乎一致的结果。



# 经典理想气体的应用实例

对于经典理想气体，忽略气体自我体积以及气体分子间的相互作用，忽略外力。因此系统的能量为
$$H = \sum_{r} \frac{p_{r}^{2}}{2m} $$
配分函数为
$$Z = \frac{1}{N!h^{3N}} \int \exp\left( -\frac{\beta}{2m}\sum_{i} p_{i}^{2} \right) d\Gamma $$
对空间坐标的积分是相当容易的，对于这些积分的任一个，我们都得到一个因子 $V^N$ 
$$Z = \frac{V^{N}}{N!h^{3N}} \int \exp\left( -\frac{\beta}{2m} \sum_{i}p_{i}^{2} \right) d^{3N}p $$
对动量的积分也是容易的，因为被积函数中的不同动量变量无耦合所以完全是同一个积分进行 $3N$ 次
$$Z = \frac{V^{N}}{N!h^{3N}}\left( \int \exp\left( -\frac{\beta}{2m}p^{2} \right) dp\right)^{3N} = \frac{1}{N!}\left[  \frac{V}{\hbar}(2\pi mkT)^{3/2}  \right]^{N} $$
由此快速得到亥姆霍兹自由能
$$A = -kT\ln A \approx NkT \left\{ \ln\left[ \frac{N}{V}\left( \frac{h^{2}}{2\pi mkT} \right)^{3/2} \right] -1 \right\} $$
$$\mu = \left( \frac{\partial A}{\partial N} \right) = kT\ln\left[ \frac{N}{V}\left( \frac{h^{2}}{2\pi mkT} \right)^{3/2} \right] $$
$$P = -\left( \frac{\partial A}{\partial V} \right) = \frac{NkT}{V} $$
$$S = -\left( \frac{\partial A}{\partial T} \right) = Nk\left\{  \ln\left[ \frac{V}{N}\left( \frac{2\pi mkT}{h^{2}} \right)^{3/2} \right] + \frac{5}{2}  \right\} $$
$$U = -\frac{\partial \ln Z}{\partial \beta} = A + TS = \frac{3}{2}NkT $$

有一个很有意思的事情是，我们好像能分离出来单个粒子的配分函数
$$Z_{N} = \frac{1}{N!}(Z_{1})^{N} ,\quad Z_{1} = \frac{V}{\hbar}(2\pi mkT)^{3/2} $$

已知了配分函数，尝试推导态密度
$$ $$